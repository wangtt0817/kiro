# 妖币猎手策略深度优化文档 V3.0

> **本版核心原则**：批判性融合，不堆砌指标。  
> 每个 yss-signal 字段都经过 **价值评估 + 冲突检测 + 冗余去重**，  
> **只吸收能补全 V1 真实缺口的信号，剔除冗余/反指标/逻辑冲突项**。

日期：2026-05-26  
基础策略：bitdong/妖币猎手 V1.0  
新数据源：yss-signal.com（已通过截图确认实际字段）

---

## 一、评估方法论

对每个外部信号，问三个问题：

1. **它解决了 V1 的什么真实缺口？**（如果解决不了任何缺口，剔除）
2. **它和 V1 已有指标是否冲突或重复？**（冲突则选一方，重复则去重）
3. **在妖币策略叙事下，它是顺向信号还是反指标？**（反指标要反向使用）

只有同时通过这三关的字段，才进入 V3 打分体系。

---

## 二、yss-signal 字段逐项评估表

| # | yss字段 | V1 缺口? | 冲突/重复? | 妖币叙事下方向 | **裁定** |
|---|--------|---------|-----------|--------------|---------|
| 1 | 综合分 (77) | 否 | 黑箱重复 | 中性 | ❌ **剔除**：yss 自己的总分，喂进我们的打分等于把决策外包 |
| 2 | 热度 (901) | 否 | — | **反指标** | ❌ **剔除**：妖币哲学是"散户看不上的才是机会"，高热度反而是"已被发现"的反向信号 |
| 3 | 费率雷达分 (80) | 部分 | 与 funding 单点冗余 | 中性 | ⚠️ **降级使用**：仅作"过热熔断"的辅助判据，不参与主打分 |
| 4 | OI雷达分 (71) | — | 与 OI分(71) 完全重复 | — | ❌ **去重**：保留一个即可 |
| 5 | OI分 (71) | — | — | — | ⚠️ **降级**：仅作展示，不参与打分（黑箱评分） |
| 6 | **OI 多窗口 (5m/1h/6h/24h/30d)** | ✅ 核心缺口 | 不冲突 | 顺向 | ✅ **吸收**：V1 最大短板（无时序），尤其 30d×6h 反差信号 |
| 7 | 24h 涨幅 | 否 | 与 priceChange24h 重复 | — | ❌ **去重**：V1 已有 |
| 8 | 主线 (合约启动/DEX/底部) | ✅ | 与 V1 启动判断冲突 | 顺向 | ✅ **吸收**：替代 V1 状态机里粗暴的"涨幅≥35%"判断 |
| 9 | 主线分 (100) | 部分 | 同上 | 顺向 | ✅ **吸收**：作为状态切换的硬门槛 |
| 10 | 蓄势分 | ✅ | 不冲突 | 顺向 | ✅ **吸收**：V1 没有"洗盘时长"概念，蓄势越久爆发力越大 |
| 11 | 放量 (布尔) | 部分 | 与 dexVolToLiq 部分重复 | 顺向 | ⚠️ **辅助使用**：作为 tiebreaker，不重复加分 |
| 12 | 量能 (量能接力/衰减) | 否 | 文字标签，无量化定义 | 中性 | ⚠️ **降级**：作为状态机的 tiebreaker，不直接加分 |
| 13 | 成交额 ($13.2M) | 否 | V1 已有 futuresVolume | — | ❌ **去重** |
| 14 | **6h 量增幅 (300.7%)** | ✅ | 不冲突 | 顺向 | ✅ **吸收**：V1 没有"短期成交暴增"维度，这是主升浪启动核心信号 |
| 15 | 准备度 (70) | 部分 | 与状态机决策重叠 | 顺向 | ⚠️ **降级**：仅作"观察提醒"，不参与状态机条件 |
| 16 | 确认次数 (101) | 否 | — | **反指标** | ❌ **反向使用**：>500次 = 信号反复但从未真正启动 = 可能是假信号币，扣信心分 |
| 17 | 方向倾向 (偏多) | 否 | 与 V1 三层 bias 重叠 | 中性 | ⚠️ **降级**：仅作 V1 三层 bias 的一致性校验 |
| 18 | 当前状态 (可小仓验证) | 部分 | 与状态机重叠 | 顺向 | ⚠️ **辅助**：仅影响**仓位**，不参与状态判断 |
| 19 | 累计出现 (982次) | 否 | 与确认次数重复 | 反指标 | ❌ **剔除** |
| 20 | 强度 (高频反复) | 否 | 文字标签 | **反指标** | ❌ **剔除**：截图自带说明"高频反复≠买点" |
| 21 | **早期雷达分 + 首次发现时间** | ✅ 核心缺口 | 不冲突 | 顺向 | ✅ **吸收**：V1 完全没有"早期识别"维度，妖币的核心是早进 |
| 22 | 早期雷达状态 (强异动) | 部分 | — | 顺向 | ✅ **吸收**：作为"早期布局"状态触发条件 |
| 23 | 四道门 (4个布尔) | 部分 | 与 V1 控盘门槛冲突 | 顺向 | ⚠️ **降级**：作为辅助过滤层，不替代 V1 控盘<45 主门槛 |

---

## 三、批判性结论

### 真正进入 V3 打分体系的（5 个核心信号）

| # | 信号 | 解决 V1 什么缺口 | 进入哪一层 |
|---|------|-----------------|----------|
| A | OI 多窗口（特别是 OI_30d × OI_6h 反差） | 单点快照 → 时序识别 | 持仓分 |
| B | 6h 量增幅 | 缺"短期成交暴增"信号 | 持仓分 |
| C | 主线 + 主线分 | 状态机粗暴的涨幅判断 | 状态机硬门槛 |
| D | 蓄势分 | 缺"洗盘时长"概念 | 结构分 |
| E | 早期雷达分 + 首次发现时间 | 缺"早期识别"维度 | 状态机新增"早期布局"状态 |

### 降级使用的（4 个，不参与打分但参与决策辅助）

| # | 信号 | 用途 |
|---|------|------|
| F | 费率雷达分 | 过热熔断的辅助判据 |
| G | 当前状态 / 准备度 | 仓位档位调节 |
| H | 方向倾向 | V1 三层 bias 的一致性校验，不一致时降低 confidence |
| I | 四道门 | 辅助过滤层，未通过仅警告 |

### 明确剔除的（10 个）

> **理由统一**：要么黑箱（喂回自己的打分等于决策外包），要么和 V1 重复（不增量信息），要么是反指标（妖币策略下高热度/高频反复是减分项），要么是文字标签（无量化定义）。

| # | 信号 | 剔除理由 |
|---|------|---------|
| 1 | yss 综合分 | 黑箱总分，不能喂回打分 |
| 2 | 热度 | 反指标（被散户发现= 庄家不会做） |
| 3 | OI雷达分 | 与 OI分重复 |
| 4 | OI分 | 黑箱评分 |
| 5 | 24h 涨幅 | V1 已有 |
| 6 | 成交额（绝对值） | V1 已有 |
| 7 | 量能（文字标签） | 无量化，仅作 tiebreaker |
| 8 | 累计出现 982 | 反指标 |
| 9 | 强度（高频反复） | 反指标，文字标签 |
| 10 | 确认次数 | 反向使用，不正向加分 |

### 反向使用的（1 个）

| # | 信号 | 反向逻辑 |
|---|------|---------|
| - | 确认次数 | >500 次 = 信号反复未启动 = 假信号币 → 总分 ×0.85（扣信心） |

---

## 四、V3 打分体系（精简版）

### 4.1 控盘分（权重 35%）

**保持 V1 原貌**，仅在 michill.ai 可用时替换 top10Ratio 代理。  
**不引入 yss 任何字段**（yss 不提供链上集中度数据）。

### 4.2 持仓分（权重 30%）— 仅吸收 OI 时序 + 6h 量

```python
def compute_position_score_v3(input_data, yss):
    # === V1 基础不变 ===
    oi_pressure   = clamp((input_data.get("oiToMcap") or 0) * 48, 0, 30)
    turnover      = clamp((input_data.get("futuresVolumeToMcap") or 0) * 20, 0, 20)
    funding       = input_data.get("fundingRate") or 0
    long_crowding = clamp(abs(min(funding, 0)) * 18000, 0, 20)
    short_crowding= clamp(max(funding, 0) * 12000, 0, 20)
    basis         = input_data.get("basisRate") or 0
    pc24h         = input_data.get("priceChange24h") or 0

    # === yss-signal 仅吸收 2 项 ===

    # (A) 30d × 6h 反差信号：长期阴跌 + 短期 OI 拉升 = 庄家新入场
    oi_30d = yss.get("oi_30d", 0)
    oi_6h  = yss.get("oi_6h", 0)
    reversal_bonus = 0
    if oi_30d <= -0.03 and oi_6h >= 0.10:
        reversal_bonus = 12   # 强反差信号
    elif oi_30d <= 0 and oi_6h >= 0.06:
        reversal_bonus = 6    # 弱反差信号

    # (B) 6h 量暴增（300.7% 这种是关键启动信号）
    vol_6h = yss.get("volume_6h_surge", 0)  # 倍数，1.0 = 持平
    vol_bonus = 0
    if vol_6h >= 3.0:   vol_bonus = 12
    elif vol_6h >= 2.0: vol_bonus = 8
    elif vol_6h >= 1.5: vol_bonus = 4

    long_score = (
        oi_pressure + turnover + long_crowding
        + reversal_bonus + vol_bonus
        + (10 if 0 < basis <= 0.015 else 0)
        + clamp(pc24h * 140, 0, 12)
    )
    short_score = (
        oi_pressure + turnover + short_crowding
        + (10 if basis >= 0.018 else 0)
        + clamp(pc24h * 50, 0, 15)
    )

    return {
        "value": round(clamp(max(long_score, short_score), 0, 100)),
        "bias": "long" if long_score >= short_score else "short",
    }
```

**对比 V2.1 删除项**：
- ❌ `oi_momentum = oi_6h * 0.8` — 与 reversal_bonus 重复打分
- ❌ `early_comfort` — 费率雷达分降级到风控层
- ❌ `funding_neg_hours` 加分 — V1 funding 单点已经能反映拥挤度

### 4.3 结构分（权重 20%）— 仅吸收蓄势分

```python
def compute_structure_score_v3(input_data, yss):
    # V1 原样保留
    near_high   = input_data.get("nearHighRatio") or 0
    pc24h       = input_data.get("priceChange24h") or 0
    dex_vol_liq = input_data.get("dexVolumeToLiquidity") or 0
    spot_lead   = input_data.get("spotLead") or 0
    basis       = input_data.get("basisRate") or 0

    # === yss-signal 仅吸收 1 项：蓄势分 ===
    accum = yss.get("accumulation_score") or 0  # None / 0-100
    accum_bonus = clamp(accum * 0.08, 0, 8)  # 满分 8

    long_score = (
        clamp(near_high * 28, 0, 28)
        + clamp(pc24h * 100, 0, 20)
        + clamp(dex_vol_liq * 6, 0, 22)
        + clamp(spot_lead * 70, 0, 18)
        + (10 if 0 < basis <= 0.015 else 0)
        + accum_bonus
    )
    short_score = (
        (18 if near_high >= 0.92 else 0)
        + clamp(max(pc24h - 0.18, 0) * 70, 0, 18)
        + clamp(max(-spot_lead, 0) * 70, 0, 18)
        + (18 if basis >= 0.018 else 0)
        + (12 if dex_vol_liq <= 1 else 0)
    )
    return {
        "value": round(clamp(max(long_score, short_score), 0, 100)),
        "bias": "long" if long_score >= short_score else "short",
    }
```

**对比 V2.1 删除项**：
- ❌ `launch_bonus` — 移到状态机层（避免和"启动主升状态"重复打分）
- ❌ `energy_bonus`（量能接力加 8） — 文字标签，无量化基础
- ❌ `sm_long/sm_short` — 移到控盘分（michill 数据归属一处）

### 4.4 不新增"动量分"第四层

**理由**：V2.1 的动量分 73.4 全部来自 yss 的几个雷达分加权 → 等于把 yss 的判断加权后再喂回我们的总分，**逻辑闭环嫌疑严重**。  
**V3 决定**：保持三层架构，不新增第四层。yss 雷达数据的价值已经分别融入持仓分（OI/量）和状态机（启动/早期）。

### 4.5 总分公式

```python
total = control * 0.40 + position * 0.35 + structure * 0.25  # 与 V1 一致

# 反指标惩罚：确认次数过高 → 信心折扣
confirmation = yss.get("confirmation_count", 0)
if confirmation > 500:
    total *= 0.85   # 假信号币嫌疑，扣 15% 信心
elif confirmation > 200:
    total *= 0.92
```

---

## 五、状态机 V3（吸收 yss 启动结构 + 早期雷达）

```python
def derive_strategy_state_v3(d, yss):
    """
    d   = 本地三层打分结果
    yss = yss-signal 信号
    """
    control   = d["controlScore"]
    position  = d["positionScore"]
    structure = d["structureScore"]
    pos_bias  = d["positionBias"]
    str_bias  = d["structureBias"]
    funding   = d["fundingRate"]
    near_high = d["nearHighRatio"]
    pc24h     = d["priceChange24h"]
    spot_lead = d.get("spotLead", 0)

    # 仅吸收的 yss 信号
    launch_main   = yss.get("launch_main", "")           # "合约启动"/"DEX启动"/"底部启动"
    launch_score  = yss.get("launch_score", 0)
    early_score   = yss.get("early_radar_score", 0)
    early_status  = yss.get("early_radar_status", "")
    first_dt      = yss.get("first_detected_at")          # datetime
    yss_direction = yss.get("direction", "")
    four_gates    = yss.get("four_gates_passed", False)

    # === 规则 0：控盘门槛（V1 原有） ===
    if control < 45:
        return {"state": "吸筹", "bias": "观察",
                "reason": "控盘分不够，先观察。",
                "yss_warning": (None if four_gates else "yss四道门未通过")}

    # === 规则 1：早期布局（V3 新增）===
    # yss 早期雷达强异动 + 刚发现 + 控盘已成形 + 费率转负
    import datetime
    is_fresh = first_dt and (datetime.datetime.now() - first_dt).total_seconds() < 86400 * 3
    if (early_score >= 55 and early_status == "强异动"
        and is_fresh
        and funding <= 0
        and pos_bias == "long"):
        return {"state": "早期布局", "bias": "试仓多",
                "reason": "yss早期雷达确认+72h内+费率转负，处于早期吸筹/诱空临界。"}

    # === 规则 2：合约启动确认（V3 新增）===
    # yss 主线=合约启动 且 主线分≥80 + 本地结构同步 + 方向一致
    if (launch_main == "合约启动" and launch_score >= 80
        and pos_bias == "long" and structure >= 60
        and yss_direction == "偏多"
        and pc24h >= 0.06):
        return {"state": "合约启动", "bias": "做多",
                "reason": "yss主线=合约启动满分+本地结构确认+方向一致。"}

    # === 规则 3：逼空主升（V1 原有，加 yss 一致性校验）===
    if (pos_bias == "long" and str_bias == "long"
        and position >= 70 and structure >= 70
        and (funding < 0.0005 or spot_lead > 0.08)
        and pc24h >= 0.08):
        # yss 方向一致性校验：不一致则降级
        if yss_direction == "偏空":
            return {"state": "逼空主升(yss方向不一致)", "bias": "观察",
                    "reason": "本地三层共振多头，但yss方向偏空，降级观察。"}
        return {"state": "逼空主升", "bias": "做多",
                "reason": "三层共振+现货领先/费率转负，进入主升。"}

    # === 规则 4：诱空（V1 原有）===
    if (pos_bias == "long" and position >= 60 and structure >= 55
        and funding < 0):
        return {"state": "诱空", "bias": "准备多",
                "reason": "费率转负，OI 在累积。"}

    # === 规则 5：高位派发（V1 原有，加 yss 警告）===
    if (pos_bias == "short" and control >= 70 and position >= 70
        and structure >= 65 and near_high >= 0.95
        and funding > 0.001 and pc24h > 0.18):
        return {"state": "高位派发", "bias": "准备空",
                "riskFlags": ["过热"],
                "reason": "高位+滞涨+费率热。"}

    # === 规则 6：砸盘下杀（V1 原有）===
    if (pos_bias == "short" and position >= 72 and structure >= 72
        and pc24h < -0.04):
        return {"state": "砸盘下杀", "bias": "做空",
                "reason": "结构破位+持仓重。"}

    return {"state": "吸筹", "bias": "观察", "reason": "结构未确认。"}
```

**V3 状态机相比 V1 仅增加 2 个状态**：
- `早期布局` — 用 yss 早期雷达 + 首次发现时间填补 V1 无早期识别的缺口
- `合约启动` — 用 yss 主线类型替代 V1 粗暴的 priceChange ≥ 35% 判断

**V3 不新增**：
- ❌ "派发预警"（michill 信号还未实际接入，先不写）
- ❌ "诱空萌芽"（funding_neg_hours 在 V1 现有 funding 单值已能区分）
- ❌ "合约点火"（与"合约启动"语义重叠）

---

## 六、降级信号的使用方式（不参与打分，仅辅助决策）

### 6.1 仓位档位调节（用 yss 当前状态 + 准备度）

```python
def adjust_position_size(base_size, yss):
    status = yss.get("current_status", "")
    readiness = yss.get("readiness", 50)
    
    # yss 状态决定基础档位
    if status == "可加仓":
        return base_size * 1.0
    elif status == "可小仓验证":
        return base_size * 0.4   # 先上 40%
    elif status == "观察":
        return base_size * 0.0   # 不进
    else:
        # 没有明确状态时，用准备度
        if readiness >= 80:
            return base_size * 0.8
        elif readiness >= 60:
            return base_size * 0.5
        else:
            return base_size * 0.3
```

### 6.2 过热熔断（用 yss 费率雷达分作为辅助判据）

```python
def overheat_check(pc24h, funding, near_high, yss):
    fr_radar = yss.get("funding_radar_score", 50)

    heat = 0
    heat += 25 if pc24h > 0.50 else (15 if pc24h > 0.30 else 0)
    heat += 25 if funding > 0.003 else (15 if funding > 0.001 else 0)
    heat += 20 if near_high > 0.96 else (10 if near_high > 0.92 else 0)

    # yss 费率雷达分 ≤30 = 极端拥挤（费率拥挤度量化）
    heat += 20 if fr_radar <= 30 else (10 if fr_radar <= 50 else 0)

    if heat >= 70: return "FORCE_EXIT"
    if heat >= 50: return "REDUCE_50"
    return "HOLD"
```

### 6.3 yss 方向一致性校验（影响 confidence，不影响 state）

```python
def confidence_adjustment(local_bias, yss_direction):
    # local_bias: 本地状态机给出的方向
    # yss_direction: yss 方向倾向
    yss_long  = yss_direction == "偏多"
    yss_short = yss_direction == "偏空"
    
    if local_bias in ("做多", "准备多", "试仓多"):
        if yss_long:  return 1.0       # 一致
        if yss_short: return 0.7       # 严重不一致
        return 0.85                     # 中性
    if local_bias in ("做空", "准备空"):
        if yss_short: return 1.0
        if yss_long:  return 0.7
        return 0.85
    return 1.0
```

### 6.4 四道门作为辅助过滤层（仅警告，不阻断）

```python
def warning_layer(yss):
    warnings = []
    if not yss.get("gate_universe", True):       warnings.append("yss交易宇宙未通过")
    if not yss.get("gate_early_trace", True):    warnings.append("yss早期痕迹未通过")
    if not yss.get("gate_position", True):       warnings.append("yss当前位置未通过")
    if not yss.get("gate_confirmation", True):   warnings.append("yss交易确认未通过")
    return warnings   # 只附加在输出里，不影响打分和状态
```

---

## 七、$DYDX 实例验证（V3 完整推演）

### 输入数据
```yaml
本地（V1）三层（假设）:
  控盘分: 60 (中等控盘)
  持仓分基础: ~50 (oiToMcap、turnover 等)
  结构分基础: ~50

yss-signal (实测):
  oi_30d: -5.1%
  oi_6h: +13.6%
  volume_6h_surge: 3.007x
  launch_main: "合约启动"
  launch_score: 100
  accumulation_score: None (暂无)
  early_radar_score: 58
  early_radar_status: "强异动"
  first_detected_at: 2026-05-26 (即时)
  direction: "偏多"
  current_status: "可小仓验证"
  readiness: 70
  confirmation_count: 101
  funding_radar_score: 80
  four_gates_passed: True
```

### V3 打分推演

**持仓分**（V3）：
```
基础: ~50
+ reversal_bonus: oi_30d≤-3% 且 oi_6h≥10% → +12 (强反差)
+ vol_bonus: 3.0x ≥ 3.0 → +12
─────────────────
≈ 74
bias: long
```

**结构分**（V3）：
```
基础: ~50
+ accum_bonus: 暂无 → +0
─────────────────
≈ 50
bias: long
```

**控盘分**：60（V1 原值）

**总分**：
```
60×0.40 + 74×0.35 + 50×0.25 = 24 + 25.9 + 12.5 = 62.4

confirmation_count = 101 < 200 → 不打折
最终总分 ≈ 62
```

### V3 状态机推演

```
规则 0: control=60 ≥ 45 ✓ 通过
规则 1: 早期布局
  early_score=58 ≥ 55 ✓
  early_status="强异动" ✓
  first_dt 在 72h 内 ✓
  funding=0.0001 ≤ 0 ❌ (实际接近 0 但不为负)
  → 不触发

规则 2: 合约启动确认
  launch_main="合约启动" ✓
  launch_score=100 ≥ 80 ✓
  pos_bias=long ✓
  structure=50 ≥ 60 ❌ (差 10 分)
  → 不触发

规则 3: 逼空主升
  position=74, structure=50 → 都需 ≥ 70 ❌
  → 不触发

规则 4: 诱空
  funding=0.0001 < 0 ❌
  → 不触发

→ 默认: 吸筹/观察
```

### V3 决策输出

```yaml
state: 吸筹
bias: 观察
reason: 结构未确认
yss_警告: 无 (四道门全通过)
yss_状态参考: "可小仓验证"
yss_准备度: 70

仓位建议:
  按 yss "可小仓验证" → base × 0.4
  但本地状态=吸筹/观察 → 实际仓位 = 0
  
最终决策: 不进场，加入观察池
理由: 
  1. yss 提示可小仓验证，但本地结构分仅 50（未达启动阈值）
  2. funding 接近 0 但未转负，不满足"早期布局"硬条件
  3. 整体处于"yss 看好但本地证据不足"的分歧区
  → 等待结构分上 60 或 funding 真正转负再行动
```

### 关键洞察

V2.1 在同样数据下会判定为"逼空主升做多"，V3 判定为"观察"。  
**差异来源**：V3 删除了把 yss 雷达分加权再喂回总分的"循环依赖"，回归本地证据驱动决策。yss 信号只在**它独有信息**（OI 时序、6h 量、早期雷达、启动主线）上发挥作用。

---

## 八、V3 vs V2.1 vs V1 的本质区别

| 维度 | V1 | V2.1 (失败的一版) | V3 (本版) |
|------|----|------|----|
| 数据源 | 本地 | 把 yss 全部字段都吸收 | 本地为主 + yss **5 个独有信号** |
| 打分层数 | 3 | 4（新增动量分= yss 雷达加权）| 3（保持） |
| 总分逻辑 | 透明 | 含 yss 黑箱评分 → 循环依赖 | 完全透明 |
| 状态机 | 6 个状态 | 8 个状态（新增 4 个，重叠严重） | 8 个状态（仅新增 2 个） |
| 反指标处理 | 无 | 无 | confirmation>500 信心折扣 |
| 风险 | 数据缺口 | 过拟合 + 决策外包 | 平衡 |

---

## 九、剩余的 V1 真实缺口（V3 未解决，等其他数据源）

V3 没有强行用 yss 来填补 V1 的所有缺口，因为有些是 yss 本身也没有的：

1. **链上集群分析** — V1 用 top10Ratio 代理。需要 michill.ai / Bubblemaps 数据
2. **聪明钱方向** — 需要 michill.ai 数据
3. **大户出逃实时检测** — 需要链上数据
4. **回测验证** — 需要历史数据 + 回测引擎
5. **风控仓位/止损规则** — 单独成模块（不在策略层）

这些会在后续 michill.ai 截图或其他数据源接入时再处理。

---

## 十、实施清单（V3 精简版）

```
Phase 1（数据接入，1周）
  □ 对接 yss-signal 5 个核心字段:
    - oi_30d, oi_6h
    - volume_6h_surge
    - launch_main, launch_score
    - accumulation_score
    - early_radar_score, early_radar_status, first_detected_at
  □ 对接 4 个降级字段:
    - funding_radar_score
    - current_status, readiness
    - direction
    - 四道门（4 个布尔）
  □ 显式排除 10 个剔除字段（不要写入数据库）

Phase 2（打分引擎升级，3 天）
  □ 修改 compute_position_score 添加 reversal_bonus + vol_bonus
  □ 修改 compute_structure_score 添加 accum_bonus
  □ 增加反指标惩罚: confirmation > 500 → ×0.85
  □ 单元测试: 用 $DYDX 数据验证总分 ≈ 62 + state="吸筹"

Phase 3（状态机升级，2 天）
  □ 添加"早期布局"状态
  □ 添加"合约启动"状态
  □ 添加 yss 方向一致性校验降级逻辑
  □ 测试 5 个历史妖币样本，对比 V1 vs V3 判断差异

Phase 4（辅助层，2 天）
  □ 仓位档位调节
  □ 过热熔断（接入 funding_radar_score）
  □ 四道门警告层
```

---

*文档版本：V3.0 (Critical Fusion) | 2026-05-26*  
*核心原则：批判性融合，不堆砌指标*  
*基于 yss-signal.com 真实截图分析 + V1 缺口诊断*
