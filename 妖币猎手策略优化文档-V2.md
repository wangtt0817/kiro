# 妖币猎手策略优化文档 V2.0

> 整合 michill.ai（链上智能分析）+ yss-signal.com（合约衍生品信号）  
> 基于 V1.0 三层打分 + 状态机架构，补齐**时序特征、集群识别、风控层、自动警报**四大缺口  
> 日期：2026-05-26

---

## 一、优化总览

| 维度 | V1.0 现状 | V2.0 目标 | 数据来源 |
|------|-----------|-----------|----------|
| 链上集群识别 | 缺失（仅 Top10 占比） | 关联钱包聚类 + Smart Money 标签 | **michill.ai** |
| 时序特征 | 无（单点快照） | OI 变化率 / Funding 持续时长 / 清算热力 | **yss-signal.com** |
| 风控层 | 无 | 止损 / 仓位 / 过热熔断 | 规则引擎 |
| 警报触发 | 无（人工看盘） | Webhook → Telegram / 前端弹窗 | yss-signal + 自建 |
| 回测验证 | 无 | 历史案例回放 + 命中率统计 | 本地引擎 |

---

## 二、平台能力假设与接入方式

### 2.1 michill.ai — 链上智能层

**核心能力（推定）**：

- **钱包集群分析**：识别关联钱包（同源资金、相似交易模式、时间关联）
- **Smart Money 追踪**：标记高胜率钱包的买卖行为
- **Token 安全审计**：合约风险检测（Rug 概率、权限风险）
- **持仓变动监控**：大户钱包实时进出监控
- **DEX 新币发现**：早期链上项目扫描

**接入点**：
```
michill.ai API/Webhook → 本地数据层
├── cluster_ratio      → 控盘分（替代 top10Ratio 代理值）
├── smart_money_flow   → 新增"聪明钱流入"指标
├── whale_exit_alert   → 派发扣分实时化
└── token_risk_score   → 前置安全过滤
```

### 2.2 yss-signal.com — 衍生品信号层

**核心能力（推定）**：
- **OI 异动警报**：OI 在 1h/4h 窗口突变超阈值时推送
- **资金费率预警**：连续 N 周期负费率 / 极端正费率触发
- **清算热力图**：多空清算集中区间可视化 + 阈值告警
- **爆仓瀑布检测**：大规模连环清算实时推送
- **基差监控**：永续 vs 现货价差异常检测

**接入点**：
```
yss-signal.com Webhook/API → 本地数据层
├── oi_surge_1h / oi_surge_4h    → 持仓分时序增强
├── funding_streak_negative      → 诱空确认信号
├── liquidation_heatmap          → 清算密集区定位
├── cascade_alert                → 主升浪实时确认
└── basis_anomaly                → 结构分增强
```

---

## 三、优化后的四层打分系统（原三层 + 新增"动量层"）

### 3.0 新增：安全预过滤（Gate 0）

在进入三层打分之前，增加一道安全门：


```python
def safety_gate(token_data: dict) -> bool:
    """michill.ai 安全审计前置过滤"""
    risk_score = token_data.get("michill_risk_score", 0)  # 0-100, 越高越危险
    if risk_score >= 75:
        return False  # 直接拒绝：高 Rug 风险
    if token_data.get("has_mint_authority", False):
        return False  # 可无限增发
    if token_data.get("has_freeze_authority", False):
        return False  # 可冻结钱包
    return True
```

### 3.1 控盘分优化（权重 35%，原 40%）

**关键变更**：用 michill.ai 的集群数据**替代**代理模型

| 子项 | V1.0 | V2.0 | 数据源 |
|------|------|------|--------|
| 流通率 | 不变 | 不变 | Dexscreener |
| 流动性压力 | 不变 | 不变 | Dexscreener |
| 地址稀缺 | holders | holders + **unique_cluster_count** | michill.ai |
| 新鲜度 | 不变 | 不变 | 链上 |
| 场地错位 | 不变 | 不变 | Binance |
| **集中度** | top10Ratio (代理) | **cluster_control_ratio** (实际) | **michill.ai** |
| **派发扣分** | exchangeInflowRatio (粗) | **whale_exit_volume_24h / mcap** | **michill.ai** |
| **新增：聪明钱流入** | 无 | smart_money_net_inflow > 0 → +8/+5/+3 | **michill.ai** |

```python
def compute_control_score_v2(input_data: dict) -> dict:
    # ... 原有子项不变 ...
    
    # 替换：用 michill 集群真实数据
    cluster_ratio = input_data.get("cluster_control_ratio")  # michill.ai
    if cluster_ratio is not None:
        verified = score_by_steps(cluster_ratio, [
            (lambda v: v >= 0.88, 20),
            (lambda v: v >= 0.75, 16),
            (lambda v: v >= 0.60, 12),
            (lambda v: v >= 0.45, 8),
        ], 0)
    else:
        # 降级：仍用 top10Ratio 代理
        verified = legacy_top10_scoring(input_data)
    
    # 替换：用 michill 大户出逃实时数据
    whale_exit_ratio = input_data.get("whale_exit_volume_24h", 0) / max(input_data.get("mcap", 1), 1)
    distribution_penalty = score_by_steps(whale_exit_ratio, [
        (lambda v: v >= 0.08, 20),   # 更严格
        (lambda v: v >= 0.04, 14),
        (lambda v: v >= 0.02, 8),
        (lambda v: v >= 0.01, 4),
    ], 0)
    
    # 新增：聪明钱流入加分
    sm_inflow = input_data.get("smart_money_net_inflow_24h", 0)
    sm_bonus = score_by_steps(sm_inflow, [
        (lambda v: v >= 50000, 8),   # >5万U 净流入
        (lambda v: v >= 20000, 5),
        (lambda v: v >= 5000, 3),
    ], 0)
    
    # 总分 = 原项 + 集中度 + SM加分 - 派发扣分
    # ...
```

### 3.2 持仓分优化（权重 30%，原 35%）

**关键变更**：接入 yss-signal 时序数据


| 子项 | V1.0 | V2.0 | 数据源 |
|------|------|------|--------|
| OI 压力 | oiToMcap (快照) | oiToMcap + **oi_change_rate_4h** | yss-signal |
| 合约换手 | 不变 | 不变 | Binance |
| Long 拥挤 | funding 单点 | funding + **funding_negative_hours** | **yss-signal** |
| Short 拥挤 | funding 单点 | funding + **funding_positive_streak** | **yss-signal** |
| **新增：OI 突变** | 无 | oi_surge_1h > 30% → +12 | **yss-signal** |
| **新增：清算密集度** | 无 | liquidation_cluster_proximity → +8 | **yss-signal** |
| 基差/涨幅 | 不变 | 不变 | Binance |

```python
def compute_position_score_v2(input_data: dict) -> dict:
    # 原有逻辑
    oi_pressure = clamp((input_data.get("oiToMcap") or 0) * 48, 0, 30)
    turnover = clamp((input_data.get("futuresVolumeToMcap") or 0) * 20, 0, 20)
    funding = input_data.get("fundingRate") or 0
    
    # === yss-signal 时序增强 ===
    
    # OI 4h 变化率：正值 = OI 在增加（新仓涌入）
    oi_change_4h = input_data.get("oi_change_rate_4h", 0)  # yss-signal
    oi_momentum = clamp(oi_change_4h * 40, 0, 12)  # 最高加 12 分
    
    # Funding 持续负值时长（小时）：越久 = 诱空越深
    funding_neg_hours = input_data.get("funding_negative_hours", 0)  # yss-signal
    long_crowding_enhanced = clamp(abs(min(funding, 0)) * 18000, 0, 20)
    if funding_neg_hours >= 24:
        long_crowding_enhanced = min(long_crowding_enhanced + 8, 28)  # 持续一天加分
    elif funding_neg_hours >= 8:
        long_crowding_enhanced = min(long_crowding_enhanced + 4, 24)
    
    # OI 突变检测（1h 内 OI 涨幅 > 30%）
    oi_surge = input_data.get("oi_surge_1h", 0)  # yss-signal 推送
    surge_bonus = 12 if oi_surge > 0.3 else (6 if oi_surge > 0.15 else 0)
    
    # 清算密集区接近度（当前价距最近清算集中带的距离百分比）
    liq_proximity = input_data.get("liquidation_cluster_proximity", 1.0)  # yss-signal
    # 越接近 0 = 价格越接近大量清算单 = 即将触发连环爆
    liq_bonus = 8 if liq_proximity <= 0.02 else (4 if liq_proximity <= 0.05 else 0)
    
    long_score = (
        oi_pressure + turnover + long_crowding_enhanced 
        + oi_momentum + surge_bonus + liq_bonus
        + (10 if basis > 0 and basis <= 0.015 else 0)
        + clamp(price_change * 140, 0, 12)
    )
    # ... short_score 类似 ...
```

### 3.3 结构分优化（权重 20%，原 25%）

**关键变更**：引入 michill Smart Money 方向确认

| 子项 | V1.0 | V2.0 | 数据源 |
|------|------|------|--------|
| 接近高位 | 不变 | 不变 | Dexscreener |
| 24h 涨幅 | 不变 | 不变 | Binance |
| DEX 量/流动性 | 不变 | 不变 | Dexscreener |
| 现货领先 | spotLead | spotLead + **sm_buying_pressure** | **michill.ai** |
| **新增：聪明钱方向** | 无 | SM 净买入 → long +10 / SM 净卖出 → short +10 | **michill.ai** |
| **新增：链上活跃度** | 无 | 新增独立买家钱包数趋势 | **michill.ai** |


```python
def compute_structure_score_v2(input_data: dict) -> dict:
    # 原有逻辑 ...
    
    # === michill.ai 增强 ===
    
    # 聪明钱方向：正 = SM 在买入，负 = SM 在卖出
    sm_direction = input_data.get("smart_money_net_direction", 0)  # michill.ai
    sm_long_bonus = clamp(sm_direction * 50, 0, 10) if sm_direction > 0 else 0
    sm_short_bonus = clamp(abs(sm_direction) * 50, 0, 10) if sm_direction < 0 else 0
    
    # 链上新买家趋势（24h 内新增独立买家数 / 总 holders）
    new_buyer_ratio = input_data.get("new_buyers_24h_ratio", 0)  # michill.ai
    activity_bonus = clamp(new_buyer_ratio * 200, 0, 8)  # 新买家涌入加 long 分
    
    long_score = (
        clamp(near_high * 28, 0, 28)
        + clamp(price_change * 100, 0, 20)
        + clamp(dex_volume_to_liquidity * 6, 0, 22)
        + clamp(spot_lead * 70, 0, 18)
        + (10 if basis > 0 and basis <= 0.015 else 0)
        + sm_long_bonus          # 新增
        + activity_bonus         # 新增
    )
    short_score = (
        (18 if near_high >= 0.92 else 0)
        + clamp(max(price_change - 0.18, 0) * 70, 0, 18)
        + clamp(max(-spot_lead, 0) * 70, 0, 18)
        + (18 if basis >= 0.018 else 0)
        + (12 if dex_volume_to_liquidity <= 1 else 0)
        + sm_short_bonus         # 新增
    )
```

### 3.4 新增：动量分（权重 15%，新层）

**目的**：捕捉"状态切换临界点"，弥补快照打分无法感知趋势加速/减速的短板。

```python
def compute_momentum_score(input_data: dict) -> dict:
    """
    完全基于 yss-signal 时序数据构建的第四层
    评估：趋势正在加速还是衰减
    """
    # OI 加速度：4h OI 变化 vs 24h OI 变化
    oi_accel = input_data.get("oi_acceleration", 0)  # yss-signal: (oi_4h_chg - oi_24h_chg/6)
    accel_score = clamp(oi_accel * 80, -15, 20)
    
    # 资金费率趋势：最近 3 次 funding 的斜率
    funding_slope = input_data.get("funding_slope_3periods", 0)  # yss-signal
    funding_trend = clamp(funding_slope * 5000, -10, 15)
    # 负斜率 = funding 在变得更负 = 诱空加深 → long 加分
    
    # 清算事件频率：最近 4h 内爆仓笔数 / 通常 4h 均值
    liq_freq_ratio = input_data.get("liquidation_frequency_ratio_4h", 1.0)  # yss-signal
    liq_momentum = clamp((liq_freq_ratio - 1) * 20, 0, 15)
    # >1 说明爆仓正在加速
    
    # 成交量加速（合约 volume 4h vs 24h 均值）
    vol_accel = input_data.get("volume_acceleration_ratio", 1.0)  # yss-signal
    vol_score = clamp((vol_accel - 1) * 15, 0, 12)
    
    # 综合
    long_momentum = max(accel_score, 0) + max(-funding_trend, 0) + liq_momentum + vol_score
    short_momentum = max(-accel_score, 0) + max(funding_trend, 0) + vol_score
    
    value = clamp(max(long_momentum, short_momentum), 0, 100)
    bias = "long" if long_momentum >= short_momentum else "short"
    
    return {"value": int(value), "bias": bias, "breakdown": {...}}
```

### 3.5 新权重分配

```
V1.0: total = control*0.40 + position*0.35 + structure*0.25
V2.0: total = control*0.35 + position*0.30 + structure*0.20 + momentum*0.15
```


> 注意：如果 michill / yss-signal 数据不可用，自动降级回 V1.0 权重和逻辑。

---

## 四、状态机优化

### 4.1 新增状态 & 修改条件

```python
def derive_strategy_state_v2(input_data: dict) -> dict:
    control  = input_data["controlScore"]
    position = input_data["positionScore"]
    structure = input_data["structureScore"]
    momentum = input_data.get("momentumScore", 0)  # 新增
    pos_bias = input_data["positionBias"]
    str_bias = input_data["structureBias"]
    mom_bias = input_data.get("momentumBias", "neutral")  # 新增
    funding  = input_data["fundingRate"]
    near_high = input_data["nearHighRatio"]
    pc24h    = input_data["priceChange24h"]
    spot_lead = input_data.get("spotLead", 0)
    
    # === yss-signal 时序条件 ===
    funding_neg_hours = input_data.get("funding_negative_hours", 0)
    oi_surge_1h = input_data.get("oi_surge_1h", 0)
    cascade_alert = input_data.get("cascade_alert", False)  # 爆仓瀑布触发
    
    # === michill.ai 条件 ===
    sm_buying = input_data.get("smart_money_net_direction", 0) > 0
    whale_exiting = input_data.get("whale_exit_volume_24h", 0) > input_data.get("mcap", 1) * 0.03
    
    # ------ 规则 0: 安全门 ------
    if control < 45:
        return {"state": "吸筹", "bias": "观察", "riskFlags": ["控盘不足"],
                "reason": "控盘分不够，先观察。"}
    
    # ------ 规则 0.5: 新增 - 大户出逃熔断 ------
    if whale_exiting and near_high >= 0.85:
        return {"state": "派发预警", "bias": "回避",
                "riskFlags": ["大户出逃"],
                "reason": "michill 检测到大户正在出货，回避。"}
    
    # ------ 规则 1: 启动主升（加强条件）------
    if (pc24h >= 0.35 and structure >= 60 and control >= 45
        and (pos_bias == "long" or mom_bias == "long")  # V2: 必须多头确认
        and not whale_exiting):                          # V2: 排除正在出货
        risk_flags = []
        if pc24h > 0.8 and near_high > 0.96:
            risk_flags.append("过热")
        return {"state": "启动主升", "bias": "准备多", "riskFlags": risk_flags,
                "reason": "价格强势启动，动量+持仓同步确认多头。"}
    
    # ------ 规则 2: 逼空主升（接入清算瀑布确认）------
    if (pos_bias == "long" and str_bias == "long"
        and position >= 70 and structure >= 70
        and (funding < 0.0005 or spot_lead > 0.08)
        and pc24h >= 0.08
        and (cascade_alert or oi_surge_1h > 0.2)):  # V2: 需要爆仓或OI突变确认
        risk_flags = []
        if pc24h > 0.6 and near_high > 0.96:  # V2: 过热阈值下调
            risk_flags.append("过热")
        return {"state": "逼空主升", "bias": "做多", "riskFlags": risk_flags,
                "reason": "三层共振+爆仓瀑布/OI突变确认，进入主升。"}
    
    # ------ 规则 3: 诱空（加时序确认）------
    if (pos_bias == "long" and position >= 60 and structure >= 55
        and funding < 0
        and funding_neg_hours >= 8     # V2: 需持续负费率 ≥8h
        and sm_buying):                 # V2: 聪明钱在买
        return {"state": "诱空", "bias": "准备多", "riskFlags": [],
                "reason": "费率持续为负 + 聪明钱流入，庄在蓄力。"}
    
    # ------ 规则 3.5: 新增 - 诱空早期（仅观察）------
    if (pos_bias == "long" and position >= 55 and funding < 0
        and funding_neg_hours >= 4 and funding_neg_hours < 8):
        return {"state": "诱空萌芽", "bias": "观察",  "riskFlags": [],
                "reason": "费率刚转负，密切关注是否持续。"}
    
    # ------ 规则 4: 高位派发（加 michill 确认）------
    if (pos_bias == "short" and control >= 70 and position >= 70
        and structure >= 65 and near_high >= 0.95
        and funding > 0.001 and pc24h > 0.18
        and (whale_exiting or not sm_buying)):  # V2: 链上确认出货
        return {"state": "高位派发", "bias": "准备空", "riskFlags": ["过热"],
                "reason": "高位+链上出货信号确认，庄在派发。"}
    
    # ------ 规则 5: 砸盘下杀 ------
    if (pos_bias == "short" and position >= 72 and structure >= 72
        and pc24h < -0.04):
        return {"state": "砸盘下杀", "bias": "做空", "riskFlags": [],
                "reason": "结构破位+持仓仍重，下杀段。"}
    
    # ------ 规则 6: 默认 ------
    return {"state": "吸筹", "bias": "观察", "riskFlags": [],
            "reason": "结构未确认，继续盯盘。"}
```

### 4.2 状态流转图（V2 新增节点用 ★ 标记）

```
                    ┌─────────────────────────────────┐
                    │           安全过滤              │
                    │   michill risk_score < 75       │
                    └───────────────┬─────────────────┘
                                    │ PASS
                    ┌───────────────▼─────────────────┐
                    │         控盘分 ≥ 45?            │
                    └───┬───────────────────────┬─────┘
                   NO   │                       │ YES
              ┌─────────▼───┐     ┌─────────────▼──────────────┐
              │   吸筹/观察 │     │   ★ 大户出逃检测           │
              └─────────────┘     │   whale_exiting?            │
                                  └───┬──────────────────┬──────┘
                                 YES  │                  │ NO
                          ┌───────────▼───┐    ┌────────▼────────────┐
                          │ ★派发预警/回避│    │   启动主升判断       │
                          └───────────────┘    │   (V2: +bias确认)    │
                                               └───┬────────────┬────┘
                                              YES  │            │ NO
                                    ┌──────────────▼──┐  ┌──────▼────────────┐
                                    │   启动主升/多   │  │  逼空主升判断      │
                                    └─────────────────┘  │  (V2: +cascade)   │
                                                         └──┬──────────┬─────┘
                                                       YES  │          │ NO
                                              ┌─────────────▼──┐  ┌───▼────────────┐
                                              │  逼空主升/做多 │  │  诱空判断       │
                                              └────────────────┘  │(V2:+8h+SM)     │
                                                                  └──┬─────────┬───┘
                                                                YES  │         │NO
                                                     ┌───────────────▼──┐  ┌───▼────────┐
                                                     │   诱空/准备多    │  │★诱空萌芽   │
                                                     └──────────────────┘  │  或高位派发 │
                                                                           │  或砸盘下杀 │
                                                                           │  或吸筹     │
                                                                           └─────────────┘
```


---

## 五、风控层（全新模块）

### 5.1 仓位管理

```python
POSITION_RULES = {
    "max_single_exposure_pct": 0.05,    # 单币最大仓位 = 总资金 5%
    "max_total_meme_exposure": 0.20,     # 所有妖币合计 ≤ 20%
    "entry_scaling": [0.3, 0.3, 0.4],   # 三档建仓（30%/30%/40%）
    "leverage_cap": 5,                   # 最大杠杆 5x
}

def calc_position_size(total_equity, signal_strength, total_score):
    """基于信号强度动态调仓"""
    base = total_equity * POSITION_RULES["max_single_exposure_pct"]
    # total_score 60-80 用 base*0.6，80-90 用 base*0.8，90+ 用 base*1.0
    if total_score >= 90:
        return base * 1.0
    elif total_score >= 80:
        return base * 0.8
    elif total_score >= 70:
        return base * 0.6
    else:
        return base * 0.4
```

### 5.2 止损规则

```python
STOP_LOSS_RULES = {
    "逼空主升": {
        "initial_stop": -0.08,           # 入场后跌 8% 止损
        "trailing_stop_activate": 0.15,  # 浮盈 15% 激活追踪止损
        "trailing_stop_distance": 0.10,  # 追踪止损距最高点 10%
    },
    "诱空": {
        "initial_stop": -0.06,           # 更紧的止损（还未确认主升）
        "time_stop_hours": 48,           # 48h 未启动主升则平仓
    },
    "启动主升": {
        "initial_stop": -0.10,
        "trailing_stop_activate": 0.20,
        "trailing_stop_distance": 0.12,
    },
    "砸盘下杀": {
        "initial_stop": -0.08,           # 做空止损
        "trailing_stop_activate": 0.12,
        "trailing_stop_distance": 0.08,
    },
}
```

### 5.3 过热熔断（V2 强化）

```python
def check_overheat_circuit_breaker(input_data: dict) -> dict:
    """
    V1.0 只在 priceChange>80% && nearHigh>96% 时标 flag
    V2.0 提前介入，且可触发强制减仓
    """
    pc = input_data.get("priceChange24h", 0)
    funding = input_data.get("fundingRate", 0)
    near_high = input_data.get("nearHighRatio", 0)
    lsur = input_data.get("long_short_ratio", 1.0)
    
    heat_score = 0
    heat_score += 25 if pc > 0.50 else (15 if pc > 0.30 else 0)
    heat_score += 25 if funding > 0.003 else (15 if funding > 0.001 else 0)
    heat_score += 20 if near_high > 0.96 else (10 if near_high > 0.92 else 0)
    heat_score += 20 if lsur > 2.5 else (10 if lsur > 1.8 else 0)
    
    if heat_score >= 70:
        return {"action": "FORCE_EXIT", "reason": "极度过热，强制清仓"}
    elif heat_score >= 50:
        return {"action": "REDUCE_50", "reason": "过热，减仓50%"}
    elif heat_score >= 35:
        return {"action": "TIGHTEN_STOP", "reason": "偏热，收紧止损"}
    else:
        return {"action": "HOLD", "reason": "正常"}
```

---

## 六、警报系统设计（yss-signal + michill 双源）

### 6.1 Webhook 事件流

```
┌─────────────┐         ┌──────────────────┐         ┌───────────────┐
│ yss-signal  │────────►│  Local Webhook   │────────►│  Strategy     │
│ (衍生品)    │  push   │  Receiver        │  enrich │  Engine V2    │
└─────────────┘         │                  │         └───────┬───────┘
                        │                  │                 │
┌─────────────┐         │                  │         ┌───────▼───────┐
│ michill.ai  │────────►│                  │────────►│  Alert Router │
│ (链上)      │  push   └──────────────────┘         └───────┬───────┘
└─────────────┘                                              │
                                                    ┌────────┼────────┐
                                                    ▼        ▼        ▼
                                              Telegram   Web弹窗   日志/DB
```

### 6.2 触发规则表

| 事件 | 来源 | 触发条件 | 推送内容 |
|------|------|----------|----------|
| 诱空启动 | yss-signal | funding < 0 连续 8h + OI 增 > 15% | 币种 + 当前三层分 + 建议动作 |
| OI 突变 | yss-signal | 1h OI 变化 > 30% | 币种 + OI 数据 + 清算热力位 |
| 爆仓瀑布 | yss-signal | 4h 爆仓额 > OI * 5% | 币种 + 方向 + 建议追入/观望 |
| 大户出逃 | michill.ai | whale_exit > mcap * 3% | 币种 + 出逃钱包 + 减仓建议 |
| 聪明钱涌入 | michill.ai | SM 净买入 > $50k + 控盘分 ≥ 60 | 币种 + SM 钱包列表 + 评分 |
| 过热熔断 | 本地引擎 | heat_score ≥ 50 | 币种 + 热力分 + 减仓/清仓指令 |
| 状态跃迁 | 本地引擎 | state 从 X 变为 Y | 旧状态→新状态 + 理由 |

---

## 七、数据流水线架构


```
┌────────────────── 数据源层 ──────────────────────────────────────┐
│                                                                    │
│  Binance Futures API    Dexscreener API    michill.ai    yss-signal│
│  (OI/Funding/Basis/    (DEX vol/liq/     (集群/SM/     (时序OI/   │
│   Volume/Price)         holders/price)    鲸鱼/风险)   清算/FR趋势)│
│                                                                    │
└────────┬───────────────────┬──────────────────┬──────────────┬────┘
         │                   │                  │              │
         ▼                   ▼                  ▼              ▼
┌────────────────── 数据标准化层 ───────────────────────────────────┐
│                                                                    │
│  data_normalizer.py                                                │
│  - 统一时间戳对齐（UTC）                                          │
│  - 缺失值标记 + 降级策略选择                                      │
│  - 异常值过滤（OI 闪崩 / API 错误）                               │
│                                                                    │
└────────────────────────────┬──────────────────────────────────────┘
                             │
                             ▼
┌────────────────── 策略计算层 ──────────────────────────────────────┐
│                                                                    │
│  ┌────────┐ ┌────────┐ ┌─────────┐ ┌──────────┐ ┌────────────┐  │
│  │Gate 0  │→│控盘分  │→│持仓分   │→│结构分    │→│动量分      │  │
│  │安全门  │ │V2      │ │V2       │ │V2        │ │(新)        │  │
│  └────────┘ └────────┘ └─────────┘ └──────────┘ └────────────┘  │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │              状态机 V2 (derive_strategy_state_v2)             │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                                                                    │
└────────────────────────────┬──────────────────────────────────────┘
                             │
                             ▼
┌────────────────── 决策执行层 ──────────────────────────────────────┐
│                                                                    │
│  ┌────────────┐  ┌───────────────┐  ┌─────────────────────────┐  │
│  │ 风控熔断   │  │ 仓位计算器    │  │ 警报路由 → TG/Web/Log  │  │
│  │ circuit_   │  │ position_     │  │ alert_router.py          │  │
│  │ breaker    │  │ sizer         │  │                          │  │
│  └────────────┘  └───────────────┘  └─────────────────────────┘  │
│                                                                    │
└───────────────────────────────────────────────────────────────────┘
```

---

## 八、降级策略（Graceful Degradation）

当某个外部平台不可用时，系统自动降级：

| 场景 | 降级行为 |
|------|----------|
| michill.ai 不可用 | 控盘分回退到 V1 代理模型（top10Ratio 等）；SM/集群指标置 null，不参与打分；权重自动重新分配为 V1 比例 |
| yss-signal 不可用 | 动量分 = 0，不参与加权；持仓分退回 V1 单点快照模式；清算/OI突变条件从状态机中跳过 |
| 两者均不可用 | 完全退回 V1.0 逻辑，保持原有功能 |
| 单个 API 限流 | 使用本地缓存最近一次有效数据（TTL: 5min），标记 stale flag |

```python
def compute_total_score_v2(scores: dict, available_sources: set) -> int:
    if "michill" in available_sources and "yss" in available_sources:
        # 全量模式
        return floor(
            scores["control"] * 0.35 +
            scores["position"] * 0.30 +
            scores["structure"] * 0.20 +
            scores["momentum"] * 0.15
        )
    elif "yss" in available_sources:
        # 无 michill，结构分降权
        return floor(
            scores["control"] * 0.38 +
            scores["position"] * 0.32 +
            scores["structure"] * 0.18 +
            scores["momentum"] * 0.12
        )
    elif "michill" in available_sources:
        # 无 yss，动量分为 0
        return floor(
            scores["control"] * 0.40 +
            scores["position"] * 0.35 +
            scores["structure"] * 0.25
        )
    else:
        # V1.0 模式
        return floor(
            scores["control"] * 0.40 +
            scores["position"] * 0.35 +
            scores["structure"] * 0.25
        )
```

---

## 九、回测框架设计

### 9.1 历史案例库

| 币种 | 日期 | 结果 | 预期状态路径 |
|------|------|------|-------------|
| RAVE | 2026-04 | 逼空主升成功 | 吸筹→诱空→逼空主升→高位派发 |
| SIREN | 2026-03 | 派发后砸盘 | 吸筹→诱空→启动主升→高位派发→砸盘 |
| (待补充) | - | - | - |

### 9.2 回测指标

```python
BACKTEST_METRICS = {
    "hit_rate": "状态判断准确率（实际走势 vs 预测状态）",
    "pnl_per_signal": "每个信号的平均盈亏（含止损）",
    "max_drawdown": "单次最大回撤",
    "sharpe_ratio": "夏普比率（日度）",
    "avg_hold_hours": "平均持仓时间",
    "false_positive_rate": "误判率（判多实际跌 / 判空实际涨）",
    "state_transition_accuracy": "状态跃迁预测准确率",
}
```

### 9.3 回测方法

1. 收集历史币种的分钟级数据（Price / OI / Funding / Volume / DEX data）
2. 按 5min 粒度回放，每个 tick 调用 `compute_strategy_bundle_v2`
3. 记录每次状态变更时间点 + 实际后续价格表现
4. 统计命中率和盈亏比


---

## 十、实施路线图

### Phase 1（1-2 周）：数据接入

- [ ] 注册/获取 michill.ai API key，确认可用端点
- [ ] 注册/获取 yss-signal.com Webhook/API 接口
- [ ] 编写 `data_normalizer.py`：统一两个平台的数据格式
- [ ] 编写降级逻辑 + 本地缓存层
- [ ] 测试：手动喂入 2-3 个活跃币种数据，验证管道通畅

### Phase 2（2-3 周）：策略引擎升级

- [ ] 实现 `compute_control_score_v2` + `compute_position_score_v2`
- [ ] 实现 `compute_structure_score_v2` + `compute_momentum_score`
- [ ] 实现 `derive_strategy_state_v2`
- [ ] 单元测试：用 RAVE / SIREN 历史数据验证每个函数输出
- [ ] 对比 V1 vs V2 在已知案例上的判断差异

### Phase 3（1-2 周）：风控 + 警报

- [ ] 实现 `check_overheat_circuit_breaker`
- [ ] 实现 `calc_position_size` + 止损规则
- [ ] 搭建 Webhook receiver + Telegram bot 推送
- [ ] 联调：模拟事件触发 → 策略计算 → 推送警报

### Phase 4（持续）：回测 + 调参

- [ ] 收集 10+ 历史妖币数据
- [ ] 构建回测引擎
- [ ] 运行 V1 vs V2 对比回测
- [ ] 基于回测结果调整阈值和权重
- [ ] 输出回测报告

---

## 十一、关键指标对照表（API 字段映射）

### michill.ai 预期字段

| 本地字段名 | michill API 字段（推定） | 含义 |
|-----------|-------------------------|------|
| `cluster_control_ratio` | `cluster_analysis.control_ratio` | 关联集群控盘占比 |
| `smart_money_net_inflow_24h` | `smart_money.net_flow_24h` | 聪明钱 24h 净流入（USD） |
| `smart_money_net_direction` | `smart_money.direction` | SM 方向（+1 买/-1 卖） |
| `whale_exit_volume_24h` | `whale_tracker.exit_volume_24h` | 大户 24h 向交易所转出量 |
| `new_buyers_24h_ratio` | `holder_analysis.new_buyers_ratio` | 新增买家/总持有者 |
| `michill_risk_score` | `token_audit.risk_score` | 合约风险分 0-100 |

### yss-signal.com 预期字段

| 本地字段名 | yss-signal 字段（推定） | 含义 |
|-----------|------------------------|------|
| `oi_change_rate_4h` | `derivatives.oi_change_pct_4h` | OI 4h 变化率 |
| `oi_surge_1h` | `alerts.oi_surge_1h` | 1h OI 突变幅度 |
| `funding_negative_hours` | `derivatives.funding_neg_duration_h` | 连续负费率时长 |
| `funding_slope_3periods` | `derivatives.funding_slope` | 近 3 期 funding 斜率 |
| `liquidation_cluster_proximity` | `liquidation.nearest_cluster_pct` | 距最近清算集中带距离 |
| `liquidation_frequency_ratio_4h` | `liquidation.freq_ratio_4h` | 爆仓频率/均值比 |
| `cascade_alert` | `alerts.cascade_detected` | 爆仓瀑布布尔值 |
| `oi_acceleration` | `derivatives.oi_acceleration` | OI 加速度 |
| `volume_acceleration_ratio` | `derivatives.vol_accel_ratio` | 成交量加速比 |

> **注意**：以上字段名为推定映射，接入时需根据实际 API 文档调整。

---

## 十二、V1 → V2 变更摘要

| # | 变更项 | 影响 |
|---|--------|------|
| 1 | 新增安全预过滤 Gate 0 | 前置排除 Rug 风险币 |
| 2 | 控盘分：集群真实数据替代代理 | 准确度大幅提升 |
| 3 | 控盘分：新增 Smart Money 加分 | 捕捉庄家吸筹早期信号 |
| 4 | 持仓分：OI 突变 + 清算密集度 | 判断精准到爆发临界点 |
| 5 | 持仓分：Funding 持续时长 | 区分"短暂波动"与"持续诱空" |
| 6 | 结构分：SM 方向确认 | 减少虚假突破误判 |
| 7 | 新增动量层（第四层） | 捕捉加速/减速趋势转换 |
| 8 | 状态机：新增"派发预警"和"诱空萌芽" | 更细粒度的阶段识别 |
| 9 | 状态机：多条规则加时序+链上硬约束 | 减少误触发 |
| 10 | 新增完整风控层 | 止损/仓位/熔断 |
| 11 | 新增警报系统 | 双源事件驱动推送 |
| 12 | 降级机制 | 平台不可用时自动退回 V1 |

---

## 十三、风险提示

1. **平台依赖**：michill.ai 和 yss-signal.com 是第三方服务，可能出现 API 变更、服务中断、收费调整。务必保持 V1 降级路径畅通。
2. **过拟合风险**：新增指标越多，在少量历史样本上越容易过拟合。回测必须用 walk-forward（滚动窗口）而非全样本 in-sample。
3. **妖币本质风险**：即使策略完美识别了庄家阶段，仍可能因突发事件（交易所下架、监管、链上攻击）导致快速归零。**仓位管理是最后一道防线**。
4. **数据延迟**：链上数据通常有 1-3 个区块延迟，衍生品数据视 API 推送频率而定。在极端行情下（瞬间拉盘/砸盘），策略可能来不及反应。

---

*文档版本：V2.0 | 作者：Kiro AI | 基于 wangtt0817/bitdong 妖币猎手 V1.0 策略升级*
