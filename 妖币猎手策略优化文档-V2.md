# 妖币猎手策略优化文档 V2.1

> 整合 yss-signal.com（合约衍生品雷达）+ michill.ai（链上智能分析）
> 基于 V1.0 三层打分 + 状态机架构升级
> 日期：2026-05-26
> **本版已根据 yss-signal 真实界面数据修正所有字段映射**

---

## 一、yss-signal.com 真实数据结构（基于界面截图）

### 1.1 平台输出字段总览

```
yss-signal.com 完整信号体系
│
├── 综合分 (0-100)              ← 平台总评分
├── 热度 (数值)                  ← 市场关注度
│
├── 费率雷达
│   ├── 费率雷达分 (0-100)       ← 费率健康度评分
│   ├── 状态: 费率中性 / 极负费率 / 费率过热...
│   ├── fundingRate (实际值)
│   ├── OI_1h (变化率%)
│   ├── OI_6h (变化率%)
│   ├── OI_24h (变化率%)
│   └── 24h涨幅 (%)
│
├── OI 雷达
│   ├── OI雷达分 (0-100)         ← OI 综合评分
│   ├── 状态: 合约点火 / OI异动 / OI冷却...
│   ├── OI_5m (变化率%)
│   ├── OI_1h (变化率%)
│   ├── OI_6h (变化率%)
│   ├── OI_24h (变化率%)
│   ├── OI_30d (变化率%)
│   └── 24h涨幅 (%)
│
├── 启动结构
│   ├── 主线: 合约启动 / DEX启动 / 底部启动
│   ├── 主线分 (0-100)
│   ├── 蓄势分 (0-100 或 暂无)
│   └── 放量: 有/暂无
│
├── 成交/OI（庄家痕迹核心）
│   ├── OI分 (0-100)
│   ├── 量能: 量能接力 / 量能衰减...
│   ├── 成交额 ($)
│   └── 6h量增幅 (%)             ← 短期爆量检测
│
├── 跟进判断
│   ├── 准备度 (0-100)           ← 入场就绪度
│   ├── 确认次数 (整数)
│   ├── 方向倾向: 偏多 / 偏空
│   └── 当前状态: 可小仓验证 / 观察...
│
├── 出现记录
│   ├── 累计出现次数
│   ├── 强度: 高频反复 / 偶发...
│   └── 用法: 二次确认
│
├── 早期雷达
│   ├── 雷达分 (0-100)
│   ├── 状态: 强异动 / 弱异动...
│   └── 首次发现时间
│
└── 四道门（过滤系统）
    ├── 交易宇宙: 通过/未通过
    ├── 早期痕迹: 通过/未通过
    ├── 当前位置: 通过/未通过
    └── 交易确认: 通过/未通过
```



### 1.2 $DYDX 实例（2026-05-25 08:51 信号快照）

| 模块 | 字段 | 实际值 | 策略意义 |
|------|------|--------|----------|
| 总览 | 综合分 | 77 | 中上信号 |
| 总览 | 热度 | 901 | 高关注 |
| 费率雷达 | 费率雷达分 | 80 | 费率健康，不拥挤 |
| 费率雷达 | fundingRate | 0.00010 | 接近中性 |
| 费率雷达 | OI_1h | +1.6% | OI 温和增长 |
| 费率雷达 | OI_6h | +13.6% | **6h OI 激增** |
| 费率雷达 | OI_24h | +10.7% | 持续流入 |
| OI雷达 | OI雷达分 | 71 | 合约点火 |
| OI雷达 | OI_5m | -0.1% | 短期平稳 |
| OI雷达 | OI_30d | -5.1% | 长期底部→突然拉升 |
| 启动结构 | 主线 | 合约启动 | 合约先行 |
| 启动结构 | 主线分 | 100 | 启动确认满分 |
| 成交/OI | OI分 | 71 | 合约点火 |
| 成交/OI | 6h量 | 300.7% | **6h 成交暴增 3 倍** |
| 成交/OI | 量能 | 量能接力 | 持续性好 |
| 跟进判断 | 准备度 | 70 | 可以小仓 |
| 跟进判断 | 方向 | 偏多 | 多头 |
| 跟进判断 | 确认 | 101 | 多次确认 |
| 早期雷达 | 雷达分 | 58 | 强异动 |
| 四道门 | 全部 | 通过 | 满足所有过滤条件 |

---

## 二、优化后策略架构（四层打分 + yss-signal 融合）

### 2.0 安全预过滤（Gate 0）— michill.ai

```python
def safety_gate(token_data: dict) -> bool:
    """michill.ai 安全审计前置过滤"""
    risk_score = token_data.get("michill_risk_score", 0)
    if risk_score >= 75:
        return False  # 高 Rug 风险
    if token_data.get("has_mint_authority", False):
        return False
    if token_data.get("has_freeze_authority", False):
        return False
    return True
```

### 2.1 控盘分（权重 30%）

保持 V1 基础逻辑，增加 michill.ai 集群数据：

| 子项 | V1.0 | V2.1 | 数据源 |
|------|------|------|--------|
| 流通率/流动性/地址/新鲜度/场地 | 不变 | 不变 | Dexscreener/Binance |
| **集中度** | top10Ratio 代理 | cluster_control_ratio | **michill.ai** |
| **派发扣分** | exchangeInflowRatio | whale_exit_volume/mcap | **michill.ai** |
| **新增：聪明钱** | 无 | SM净流入 → +8/+5/+3 | **michill.ai** |



### 2.2 持仓分（权重 25%）— 接入 yss-signal OI雷达 + 费率雷达

**关键变更**：用 yss-signal 的多时间框架 OI 数据替代单点快照

```python
def compute_position_score_v2(input_data: dict, yss: dict) -> dict:
    """
    yss = yss-signal 实时数据
    yss["oi_radar_score"]       = OI雷达分 (0-100)
    yss["funding_radar_score"]  = 费率雷达分 (0-100)
    yss["oi_6h"]                = OI 6h 变化率
    yss["oi_24h"]               = OI 24h 变化率
    yss["oi_30d"]               = OI 30d 变化率
    yss["funding_rate"]         = 当前费率
    yss["volume_6h_surge"]      = 6h成交量增幅
    """
    # === 原有基础分 ===
    oi_pressure = clamp((input_data.get("oiToMcap") or 0) * 48, 0, 30)
    turnover = clamp((input_data.get("futuresVolumeToMcap") or 0) * 20, 0, 20)
    funding = yss.get("funding_rate") or input_data.get("fundingRate") or 0

    # === yss-signal OI 时序增强 ===
    
    # OI 6h 变化率：核心时序信号（截图中 13.6%）
    oi_6h = yss.get("oi_6h", 0)
    oi_momentum = clamp(oi_6h * 0.8, 0, 15)  # 13.6% → +10.9分

    # OI 从 30d 低位突然拉升 = 庄家新入场
    oi_30d = yss.get("oi_30d", 0)
    oi_24h = yss.get("oi_24h", 0)
    # 30d 为负但 24h 为正 = 底部启动信号
    reversal_bonus = 8 if (oi_30d < -0.03 and oi_24h > 0.08) else 0

    # 6h 成交量暴增（截图 300.7%）
    vol_surge = yss.get("volume_6h_surge", 0)
    vol_bonus = 12 if vol_surge > 2.5 else (8 if vol_surge > 1.5 else (4 if vol_surge > 0.8 else 0))

    # === 费率方向判断 ===
    long_crowding = clamp(abs(min(funding, 0)) * 18000, 0, 20)
    short_crowding = clamp(max(funding, 0) * 12000, 0, 20)

    # 费率雷达分高 + 费率中性 = 早期舒适区（加分）
    funding_radar = yss.get("funding_radar_score", 50)
    early_comfort = 6 if (funding_radar >= 75 and abs(funding) < 0.0003) else 0

    basis = input_data.get("basisRate") or 0
    price_change = input_data.get("priceChange24h") or 0

    long_score = (
        oi_pressure + turnover + long_crowding
        + oi_momentum + reversal_bonus + vol_bonus + early_comfort
        + (10 if basis > 0 and basis <= 0.015 else 0)
        + clamp(price_change * 140, 0, 12)
    )
    short_score = (
        oi_pressure + turnover + short_crowding
        + (10 if basis >= 0.018 else 0)
        + clamp(price_change * 50, 0, 15)
    )

    return {
        "value": js_round(clamp(max(long_score, short_score), 0, 100)),
        "bias": "long" if long_score >= short_score else "short",
    }
```



### 2.3 结构分（权重 20%）— 接入 yss-signal 启动结构

```python
def compute_structure_score_v2(input_data: dict, yss: dict) -> dict:
    """
    yss["launch_main"]        = 主线: "合约启动"/"DEX启动"/"底部启动"
    yss["launch_score"]       = 主线分 (0-100)
    yss["accumulation_score"] = 蓄势分 (0-100 or None)
    yss["volume_expansion"]   = 放量: True/False
    yss["energy_status"]      = 量能: "量能接力"/"量能衰减"
    """
    near_high = input_data.get("nearHighRatio") or 0
    price_change = input_data.get("priceChange24h") or 0
    dex_vol_liq = input_data.get("dexVolumeToLiquidity") or 0
    spot_lead = input_data.get("spotLead") or 0
    basis = input_data.get("basisRate") or 0

    # === yss-signal 启动结构加分 ===
    launch_score = yss.get("launch_score", 0)
    # 主线分 >= 80 = 启动确认强
    launch_bonus = clamp((launch_score - 60) * 0.3, 0, 12)  # 100分 → +12

    # 量能接力 = 持续性好
    energy = yss.get("energy_status", "")
    energy_bonus = 8 if energy == "量能接力" else (4 if energy == "量能维持" else 0)

    # 蓄势分（如果有横盘蓄势过）
    accum = yss.get("accumulation_score") or 0
    accum_bonus = clamp(accum * 0.1, 0, 8)

    # michill.ai 聪明钱方向
    sm_direction = input_data.get("smart_money_net_direction", 0)
    sm_long = clamp(sm_direction * 50, 0, 10) if sm_direction > 0 else 0
    sm_short = clamp(abs(sm_direction) * 50, 0, 10) if sm_direction < 0 else 0

    long_score = (
        clamp(near_high * 28, 0, 28)
        + clamp(price_change * 100, 0, 20)
        + clamp(dex_vol_liq * 6, 0, 22)
        + clamp(spot_lead * 70, 0, 18)
        + (10 if basis > 0 and basis <= 0.015 else 0)
        + launch_bonus + energy_bonus + accum_bonus + sm_long
    )
    short_score = (
        (18 if near_high >= 0.92 else 0)
        + clamp(max(price_change - 0.18, 0) * 70, 0, 18)
        + clamp(max(-spot_lead, 0) * 70, 0, 18)
        + (18 if basis >= 0.018 else 0)
        + (12 if dex_vol_liq <= 1 else 0)
        + sm_short
    )

    return {
        "value": js_round(clamp(max(long_score, short_score), 0, 100)),
        "bias": "long" if long_score >= short_score else "short",
    }
```



### 2.4 新增：动量分/雷达分（权重 25%）— 完全基于 yss-signal

**这是 V2 新增的第四层**，直接利用 yss-signal 的雷达体系：

```python
def compute_momentum_score(yss: dict) -> dict:
    """
    完全基于 yss-signal 的雷达数据构建
    yss["composite_score"]      = 综合分 (77)
    yss["oi_radar_score"]       = OI雷达分 (71)
    yss["funding_radar_score"]  = 费率雷达分 (80)
    yss["early_radar_score"]    = 早期雷达分 (58)
    yss["readiness"]            = 准备度 (70)
    yss["direction"]            = 方向: "偏多"/"偏空"
    yss["four_gates_passed"]    = 四道门是否全部通过
    yss["oi_6h"]                = OI 6h%
    yss["volume_6h_surge"]      = 6h量增幅
    yss["confirmation_count"]   = 确认次数
    """
    # OI 雷达贡献（满分 30）
    oi_radar = yss.get("oi_radar_score", 0)
    oi_part = clamp(oi_radar * 0.3, 0, 30)

    # 费率雷达贡献（满分 25）
    fr_radar = yss.get("funding_radar_score", 0)
    fr_part = clamp(fr_radar * 0.25, 0, 25)

    # 早期雷达贡献（满分 20）
    early_radar = yss.get("early_radar_score", 0)
    early_part = clamp(early_radar * 0.2, 0, 20)

    # 准备度贡献（满分 15）
    readiness = yss.get("readiness", 0)
    ready_part = clamp(readiness * 0.15, 0, 15)

    # 四道门加分（满分 10）
    gates = 10 if yss.get("four_gates_passed", False) else 0

    value = clamp(oi_part + fr_part + early_part + ready_part + gates, 0, 100)

    # 方向由 yss-signal 的方向倾向决定
    direction = yss.get("direction", "")
    if direction == "偏多":
        bias = "long"
    elif direction == "偏空":
        bias = "short"
    else:
        bias = "neutral"

    return {
        "value": int(value),
        "bias": bias,
        "breakdown": {
            "oi_radar": round(oi_part, 1),
            "funding_radar": round(fr_part, 1),
            "early_radar": round(early_part, 1),
            "readiness": round(ready_part, 1),
            "four_gates": gates,
        }
    }
```

**$DYDX 实例计算**：
```
oi_part     = 71 * 0.3  = 21.3
fr_part     = 80 * 0.25 = 20.0
early_part  = 58 * 0.2  = 11.6
ready_part  = 70 * 0.15 = 10.5
gates       = 10 (全通过)
─────────────────────────
动量分 = 73.4 → 73
方向 = long（偏多）
```



### 2.5 总分公式

```python
def compute_total_score_v2(scores: dict, available: set) -> int:
    if "yss" in available and "michill" in available:
        # 全量模式
        return floor(
            scores["control"] * 0.30 +
            scores["position"] * 0.25 +
            scores["structure"] * 0.20 +
            scores["momentum"] * 0.25   # yss-signal 雷达权重高
        )
    elif "yss" in available:
        # 无 michill
        return floor(
            scores["control"] * 0.30 +
            scores["position"] * 0.28 +
            scores["structure"] * 0.17 +
            scores["momentum"] * 0.25
        )
    elif "michill" in available:
        # 无 yss
        return floor(
            scores["control"] * 0.40 +
            scores["position"] * 0.35 +
            scores["structure"] * 0.25
        )
    else:
        # V1.0 降级
        return floor(
            scores["control"] * 0.40 +
            scores["position"] * 0.35 +
            scores["structure"] * 0.25
        )
```

---

## 三、状态机 V2.1 — 融合 yss-signal 四道门 + 雷达状态

```python
def derive_strategy_state_v2(input_data: dict, yss: dict) -> dict:
    control   = input_data["controlScore"]
    position  = input_data["positionScore"]
    structure = input_data["structureScore"]
    momentum  = input_data.get("momentumScore", 0)
    pos_bias  = input_data["positionBias"]
    str_bias  = input_data["structureBias"]
    mom_bias  = input_data.get("momentumBias", "neutral")
    funding   = yss.get("funding_rate") or input_data.get("fundingRate", 0)
    near_high = input_data["nearHighRatio"]
    pc24h     = input_data["priceChange24h"]
    spot_lead = input_data.get("spotLead", 0)

    # === yss-signal 条件 ===
    four_gates    = yss.get("four_gates_passed", False)
    oi_radar      = yss.get("oi_radar_score", 0)
    fr_radar      = yss.get("funding_radar_score", 0)
    readiness     = yss.get("readiness", 0)
    yss_direction = yss.get("direction", "")
    oi_6h         = yss.get("oi_6h", 0)
    launch_main   = yss.get("launch_main", "")
    launch_score  = yss.get("launch_score", 0)
    vol_6h_surge  = yss.get("volume_6h_surge", 0)

    # === michill.ai 条件 ===
    sm_buying     = input_data.get("smart_money_net_direction", 0) > 0
    whale_exiting = input_data.get("whale_exit_volume_24h", 0) > input_data.get("mcap", 1) * 0.03

    # ------ 规则 0: 控盘门槛 ------
    if control < 45:
        return {"state": "吸筹", "bias": "观察",
                "riskFlags": ["控盘不足"], "reason": "控盘分不够，先观察。"}

    # ------ 规则 0.5: 大户出逃熔断 (michill) ------
    if whale_exiting and near_high >= 0.85:
        return {"state": "派发预警", "bias": "回避",
                "riskFlags": ["大户出逃"], "reason": "链上检测到大户出货。"}

    # ------ 规则 1: 逼空主升（最强信号）------
    # yss 四道门全过 + OI雷达≥70 + 启动分≥80 + 方向偏多 + 本地三层共振
    if (four_gates
        and oi_radar >= 70
        and launch_score >= 80
        and yss_direction == "偏多"
        and pos_bias == "long" and str_bias == "long"
        and position >= 65 and structure >= 65
        and (funding < 0.0005 or spot_lead > 0.08)
        and pc24h >= 0.06):
        risk_flags = []
        if pc24h > 0.5 and near_high > 0.96:
            risk_flags.append("过热")
        return {"state": "逼空主升", "bias": "做多",
                "riskFlags": risk_flags,
                "reason": "yss四道门全通过+OI点火+本地三层共振，进入主升。"}

    # ------ 规则 2: 启动主升 ------
    if (pc24h >= 0.25
        and launch_score >= 70
        and (pos_bias == "long" or mom_bias == "long")
        and structure >= 55
        and not whale_exiting):
        risk_flags = []
        if pc24h > 0.8 and near_high > 0.96:
            risk_flags.append("过热")
        return {"state": "启动主升", "bias": "准备多",
                "riskFlags": risk_flags,
                "reason": "yss启动结构确认+价格强势，准备跟进。"}

    # ------ 规则 3: 合约点火（yss OI雷达触发）------
    if (oi_radar >= 65
        and oi_6h > 0.10
        and vol_6h_surge > 2.0
        and fr_radar >= 70
        and yss_direction == "偏多"):
        return {"state": "合约点火", "bias": "准备多",
                "riskFlags": [],
                "reason": "OI雷达点火+6h量暴增+费率健康，合约侧启动。"}

    # ------ 规则 4: 诱空 ------
    if (pos_bias == "long" and position >= 60 and structure >= 55
        and funding < 0
        and fr_radar >= 60
        and (sm_buying or readiness >= 60)):
        return {"state": "诱空", "bias": "准备多",
                "riskFlags": [],
                "reason": "负费率+OI累积+聪明钱/准备度确认。"}

    # ------ 规则 5: 诱空萌芽（早期雷达触发）------
    if (yss.get("early_radar_score", 0) >= 50
        and yss.get("early_radar_status", "") == "强异动"
        and funding <= 0
        and pos_bias == "long"):
        return {"state": "诱空萌芽", "bias": "观察",
                "riskFlags": [],
                "reason": "早期雷达强异动+费率转负，密切关注。"}

    # ------ 规则 6: 高位派发 ------
    if (pos_bias == "short" and control >= 70
        and position >= 70 and structure >= 65
        and near_high >= 0.95
        and funding > 0.001 and pc24h > 0.18
        and (whale_exiting or not sm_buying)):
        return {"state": "高位派发", "bias": "准备空",
                "riskFlags": ["过热"],
                "reason": "高位+链上出货+费率过热。"}

    # ------ 规则 7: 砸盘下杀 ------
    if (pos_bias == "short" and position >= 72 and structure >= 72
        and pc24h < -0.04):
        return {"state": "砸盘下杀", "bias": "做空",
                "riskFlags": [],
                "reason": "结构破位+持仓重。"}

    # ------ 规则 8: 默认 ------
    return {"state": "吸筹", "bias": "观察",
            "riskFlags": [],
            "reason": "结构未确认，继续盯盘。"}
```



---

## 四、yss-signal 字段映射表（真实 → 本地）

| yss-signal 界面字段 | 本地变量名 | 类型 | 示例值 |
|-------------------|-----------|------|--------|
| 综合分 | `yss_composite_score` | int 0-100 | 77 |
| 热度 | `yss_heat` | int | 901 |
| 费率雷达分 | `funding_radar_score` | int 0-100 | 80 |
| 费率 | `funding_rate` | float | 0.00010 |
| OI 1h | `oi_1h` | float (%) | 0.016 |
| OI 6h | `oi_6h` | float (%) | 0.136 |
| OI 24h | `oi_24h` | float (%) | 0.107 |
| OI雷达分 | `oi_radar_score` | int 0-100 | 71 |
| OI 5m | `oi_5m` | float (%) | -0.001 |
| OI 30d | `oi_30d` | float (%) | -0.051 |
| 主线 | `launch_main` | str | "合约启动" |
| 主线分 | `launch_score` | int 0-100 | 100 |
| 蓄势分 | `accumulation_score` | int/None | None |
| 放量 | `volume_expansion` | bool | False |
| OI分 | `oi_score` | int 0-100 | 71 |
| 量能 | `energy_status` | str | "量能接力" |
| 成交额 | `volume_usd` | float | 13200000 |
| 6h量增幅 | `volume_6h_surge` | float (倍) | 3.007 |
| 准备度 | `readiness` | int 0-100 | 70 |
| 确认次数 | `confirmation_count` | int | 101 |
| 方向倾向 | `direction` | str | "偏多" |
| 当前状态 | `current_status` | str | "可小仓验证" |
| 累计出现 | `total_appearances` | int | 982 |
| 强度 | `signal_intensity` | str | "高频反复" |
| 早期雷达分 | `early_radar_score` | int 0-100 | 58 |
| 早期状态 | `early_radar_status` | str | "强异动" |
| 首次发现 | `first_detected_at` | datetime | 2026-05-26 |
| 四道门-交易宇宙 | `gate_universe` | bool | True |
| 四道门-早期痕迹 | `gate_early_trace` | bool | True |
| 四道门-当前位置 | `gate_position` | bool | True |
| 四道门-交易确认 | `gate_confirmation` | bool | True |
| 四道门全通过 | `four_gates_passed` | bool | True |

---

## 五、风控层

### 5.1 仓位管理（结合 yss-signal 准备度）

```python
def calc_position_size(equity, total_score, yss_readiness, yss_status):
    """
    yss_readiness: 准备度分数 0-100
    yss_status: "可小仓验证" / "可加仓" / "观察"
    """
    base = equity * 0.05  # 单币最大 5%

    # 根据 yss 状态决定仓位比例
    if yss_status == "可小仓验证":
        size_pct = 0.3   # 先上 30%
    elif total_score >= 85 and yss_readiness >= 80:
        size_pct = 1.0   # 满仓
    elif total_score >= 75:
        size_pct = 0.6
    else:
        size_pct = 0.3

    return base * size_pct
```

### 5.2 止损规则

```python
STOP_LOSS = {
    "逼空主升": {"initial": -0.08, "trail_activate": 0.15, "trail_dist": 0.10},
    "启动主升": {"initial": -0.10, "trail_activate": 0.20, "trail_dist": 0.12},
    "合约点火": {"initial": -0.06, "time_stop_h": 24},  # 24h 不启动则出
    "诱空":     {"initial": -0.06, "time_stop_h": 48},
    "砸盘下杀": {"initial": -0.08, "trail_activate": 0.12, "trail_dist": 0.08},
}
```

### 5.3 过热熔断

```python
def check_overheat(pc24h, funding, near_high, yss_fr_radar):
    heat = 0
    heat += 25 if pc24h > 0.50 else (15 if pc24h > 0.30 else 0)
    heat += 25 if funding > 0.003 else (15 if funding > 0.001 else 0)
    heat += 20 if near_high > 0.96 else (10 if near_high > 0.92 else 0)
    # yss 费率雷达分低 = 费率拥挤 = 过热
    heat += 20 if yss_fr_radar < 30 else (10 if yss_fr_radar < 50 else 0)

    if heat >= 70: return "FORCE_EXIT"
    if heat >= 50: return "REDUCE_50"
    if heat >= 35: return "TIGHTEN_STOP"
    return "HOLD"
```



---

## 六、状态流转图

```
┌─────────────────────────────────────────────────────────┐
│                  Gate 0: michill 安全过滤                │
└───────────────────────────┬─────────────────────────────┘
                            │ PASS
┌───────────────────────────▼─────────────────────────────┐
│                  控盘分 ≥ 45?                            │
└───┬───────────────────────────────────────────────┬─────┘
   NO                                              YES
┌───▼───────┐                          ┌────────────▼──────────┐
│ 吸筹/观察 │                          │ michill 大户出逃?     │
└───────────┘                          └──┬──────────────┬─────┘
                                     YES  │              │ NO
                              ┌───────────▼───┐  ┌──────▼─────────────────────┐
                              │ 派发预警/回避 │  │ yss 四道门全通过           │
                              └───────────────┘  │ + OI雷达≥70 + 启动≥80     │
                                                 │ + 方向偏多 + 本地共振?     │
                                                 └──┬──────────────────┬──────┘
                                                YES │                  │ NO
                                     ┌──────────────▼──┐    ┌─────────▼────────┐
                                     │  逼空主升/做多  │    │ 启动主升判断?    │
                                     └─────────────────┘    └──┬───────────┬───┘
                                                          YES  │           │ NO
                                              ┌────────────────▼──┐  ┌────▼──────────┐
                                              │  启动主升/准备多  │  │ OI雷达点火?   │
                                              └───────────────────┘  └──┬────────┬───┘
                                                                   YES  │        │ NO
                                                        ┌───────────────▼──┐  ┌──▼─────────┐
                                                        │ 合约点火/准备多  │  │ 诱空判断   │
                                                        └──────────────────┘  │ 或诱空萌芽 │
                                                                              │ 或高位派发 │
                                                                              │ 或砸盘下杀 │
                                                                              │ 或吸筹     │
                                                                              └────────────┘
```

---

## 七、V1 → V2.1 核心变更对照

| # | 变更 | V1.0 | V2.1 | 影响 |
|---|------|------|------|------|
| 1 | OI 时序 | 单点 oiToMcap | yss OI_5m/1h/6h/24h/30d 多窗口 | 捕捉 OI 加速/反转 |
| 2 | 费率判断 | funding 单值 | yss 费率雷达分(0-100) + 状态标签 | 区分"健康中性"vs"过热拥挤" |
| 3 | 启动确认 | priceChange≥35% | yss 主线分+启动结构+放量 | 不再只看涨幅 |
| 4 | 入场时机 | 状态机二元判断 | yss 准备度+四道门+确认次数 | 精确到"可小仓验证" |
| 5 | 动量/第四层 | 无 | yss 雷达体系综合 | 补齐趋势感知 |
| 6 | 6h量暴增 | 无 | yss 6h量增幅(300%等) | 庄家痕迹核心指标 |
| 7 | 早期发现 | 无 | yss 早期雷达(首次时间+异动分) | 抢在主升前布局 |
| 8 | 过滤系统 | 控盘<45 pass | yss 四道门 + michill 安全门 | 双重过滤 |
| 9 | 新状态 | 无 | "合约点火" / "诱空萌芽" / "派发预警" | 更细粒度 |
| 10 | 风控 | 无 | 止损+仓位+过热熔断 | 完整保护 |

---

## 八、实施路线

| 阶段 | 时间 | 任务 |
|------|------|------|
| Phase 1 | 1周 | 对接 yss-signal 数据（手动录入或爬虫/API） |
| Phase 2 | 1周 | 实现 compute_momentum_score + 状态机 V2.1 |
| Phase 3 | 1周 | 风控层 + Telegram 警报 |
| Phase 4 | 持续 | 用真实信号回测，调阈值 |

---

## 九、$DYDX 信号模拟（基于截图数据）

```
输入：
  控盘分 = 假设 60（中等控盘）
  持仓分 = OI雷达71 + 6h量300% + 费率中性 → 约 72
  结构分 = 主线分100 + 量能接力 → 约 68
  动量分 = 73（前面计算过）

总分 = 60*0.30 + 72*0.25 + 68*0.20 + 73*0.25
     = 18 + 18 + 13.6 + 18.25
     = 67.85 → 68

状态机判断：
  四道门全通过 ✓
  OI雷达 71 ≥ 70 ✓
  启动分 100 ≥ 80 ✓
  方向偏多 ✓
  但 position(72) ≥ 65 ✓, structure(68) ≥ 65 ✓
  funding 0.00010 < 0.0005 ✓
  priceChange 6.7% ≥ 6% ✓

  → 触发规则 1: "逼空主升" / 做多

  实际 yss 给出的建议："可小仓验证"
  → 建议动作：按 30% 仓位进场，设 -6% 止损
```

---

*文档版本：V2.1 | 基于 yss-signal.com 真实界面截图修正 | 2026-05-26*