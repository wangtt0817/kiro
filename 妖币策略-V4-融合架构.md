# 妖币猎手 × TradeCat 融合架构 V4.0

> **融合原则**：双引擎互补，而非二选一。  
> TradeCat = 横截面异动检测系统（"谁在动"）  
> 妖币猎手 = 纵向阶段识别系统（"庄家走到哪了"）  
> V4 把 TradeCat 当作**信号源 + 数学严谨化工具 + 维度补全**，把 V1 阶段叙事当作**决策框架**。

日期：2026-05-30  
基础：bitdong/妖币猎手 V1.0 + 交易猫 schema `tui_unified_ranking.v1` + `ranking_snapshot.v2`

---

## 一、TradeCat 字段三档裁定（48 个字段精简表）

### A 档：核心吸收（5 个，进入打分公式）

| 字段 | V1 缺口/痛点 | 融合方式 |
|------|-------------|---------|
| **5m/15m/1h/4h/1d/1w 额强度(z)** | V1 用 `oiToMcap*48` 经验系数 | **z-score 全面替代经验系数** |
| **结构形态**（多头共振/大户领先/主动买领先...）| 你的 bias 只有 long/short 两态 | **状态机硬条件矩阵** |
| **清仓/挤压风险**（清仓风险/挤压追多风险）| V1 只有"过热"一个 flag | **状态机方向校验** |
| **主动买卖比 + 大户持仓比 + 大户仓位gap** | 完全缺失"参与者结构" | **新增主动性分（不进总分，作硬约束）** |
| **额连续根数 / 量连续根数** | V1 时序无法量化"持续性" | **替代 funding_negative_hours 等时长判断** |

### B 档：辅助使用（4 个，仅作过滤/警告）

| 字段 | 用途 |
|------|------|
| 命中数 + 命中榜单 | 共振强度过滤（命中≥3 才进候选池） |
| 强度等级（极强/强/中强/观察/弱）| 信号优先级排序 |
| 流动性等级（良好/一般/紧张/危险）| 风控仓位调节（危险→减仓） |
| 数据新鲜度分 | 工程层，<60 标 stale |

### C 档：剔除（39 个）

> **统一理由**：要么对妖币赛道无效（技术指标），要么是黑箱（综合分），要么是 V1 已有（24h涨幅、成交额）。

- ❌ TradeCat 综合分（黑箱评分，喂回打分=循环依赖）
- ❌ 全部技术指标：RSI / MACD / KDJ / ADX / CCI / ATR / 布林带 %B / SuperTrend / VWAP偏离 / VPVR / 支撑阻力 / OBV / CVD / MFI（妖币是庄家驱动，技术面在控盘币上失效）
- ❌ EMA趋势（同上）
- ❌ 大资金结构（信号=买入/观望/卖出，黑箱评分）
- ❌ 期货情绪 风险值（黑箱）
- ❌ 24h涨跌幅、成交额绝对值（V1 已有）
- ❌ 量额背离%、波动强度、结构分歧分（次要派生指标）
- ❌ 主异动类型（仅作展示，不进决策）

---


## 二、V4 融合数据流

```
┌────────────────────────────────────────────────────────────┐
│                  TradeCat 异动监控（外源）                  │
│       ranking_snapshot.v2 (5min 推送 528 个合约)            │
└──────────────────────┬─────────────────────────────────────┘
                       │ 候选池筛选
                       ▼
              [命中数 ≥ 2 OR 强度 ≥ 强]
                       │
                       ▼
┌────────────────────────────────────────────────────────────┐
│             妖币本地控盘门槛（V1 保留）                     │
│         control_score < 45 → 直接退出                      │
└──────────────────────┬─────────────────────────────────────┘
                       │
                       ▼
┌────────────────────────────────────────────────────────────┐
│             V4 三层 + 主动性 联合打分                       │
│                                                             │
│   控盘分(35%) + 持仓分(30%) + 结构分(20%) + 主动性硬约束     │
│                                                             │
│   持仓分/结构分内部全部用 z-score 替代经验系数              │
└──────────────────────┬─────────────────────────────────────┘
                       │
                       ▼
┌────────────────────────────────────────────────────────────┐
│             V4 状态机（结构形态硬约束矩阵）                  │
│                                                             │
│   触发条件 = V1 阈值 ∩ TradeCat结构形态 ∩ 风险标签          │
└──────────────────────┬─────────────────────────────────────┘
                       │
                       ▼
              [仓位 + 止损 + 警报]
```

**关键**：候选池缩窄是这套融合的核心收益——
- 不再扫所有 528 币，只看 TradeCat 推送的异动 + 控盘≥45 的子集（实测应该<30 个/分钟）
- 让你的妖币策略**只在合约异动确认存在时触发**，杜绝在死币上空转

---


## 三、V4 持仓分（z-score 全面化）

### 3.1 V1 → V4 公式对照

| V1 子项 | V1 公式（经验系数） | V4 公式（z-score 化） |
|---------|---------------------|----------------------|
| OI 压力 | `clamp(oiToMcap * 48, 0, 30)` | `z_score(oi_z_4h, k=2.5) * 30` |
| 合约换手 | `clamp(futVolToMcap * 20, 0, 20)` | `z_score(vol_z_4h, k=2.0) * 20` |
| Long 拥挤 | `clamp(abs(min(funding,0)) * 18000, 0, 20)` | `clamp(funding_neg_z * 5, 0, 20)` |
| Short 拥挤 | `clamp(max(funding,0) * 12000, 0, 20)` | `clamp(funding_pos_z * 4, 0, 20)` |

### 3.2 z_score 辅助函数

```python
def z_score_to_points(z: float, k_high: float = 2.5, max_points: int = 30) -> float:
    """
    把 z-score 映射成 0~max_points 的得分
    z = k_high (典型 2.5σ) 即满分
    z <= 0 得 0 分
    """
    if z <= 0: return 0
    return min(z / k_high, 1.0) * max_points
```

### 3.3 V4 持仓分函数

```python
def compute_position_score_v4(input_data, tc):
    """
    tc = TradeCat 数据：
        tc["amt_z_4h"]      # 4h 额强度 z-score
        tc["amt_z_1d"]      # 1d 额强度 z-score
        tc["vol_z_4h"]      # 4h 量强度 z-score
        tc["consec_amt_n"]  # 额连续根数
        tc["funding_rate"], tc["basis"], tc["price_change_24h"]
    """
    # OI 压力：4h 主信号 + 1d 长周期权重
    oi_pressure = (z_score_to_points(tc["amt_z_4h"], 2.5, 22)
                 + z_score_to_points(tc["amt_z_1d"], 2.5, 8))   # 满分 30

    # 合约换手
    turnover = z_score_to_points(tc["vol_z_4h"], 2.0, 20)       # 满分 20

    # 持续性加成（V1 完全缺失）
    consec_bonus = 0
    n = tc.get("consec_amt_n", 0)
    if n >= 4: consec_bonus = 8
    elif n >= 2: consec_bonus = 4

    # Funding 拥挤（保留 V1 思路，但加 z-score 辅助）
    funding = tc["funding_rate"] or 0
    long_crowding  = clamp(abs(min(funding,0)) * 18000, 0, 20)
    short_crowding = clamp(max(funding,0) * 12000, 0, 20)

    # 基差/涨幅（V1 不变）
    basis = tc["basis"] or 0
    pc24h = tc["price_change_24h"] or 0
    long_score = (oi_pressure + turnover + long_crowding + consec_bonus
                + (10 if 0 < basis <= 0.015 else 0)
                + clamp(pc24h * 140, 0, 12))
    short_score = (oi_pressure + turnover + short_crowding
                + (10 if basis >= 0.018 else 0)
                + clamp(pc24h * 50, 0, 15))

    return {
        "value": round(clamp(max(long_score, short_score), 0, 100)),
        "bias": "long" if long_score >= short_score else "short",
    }
```

**为什么 z-score 化是真正的提升**：  
V1 的 `* 48` 在 BTC 这种大币和 RAVE 这种妖币上含义完全不同（同一个 oiToMcap 在不同币上代表不同的"异常程度"）。z-score 是相对各币种自己历史分布的标准化，**自适应不同流动性档位的币**。

---


## 四、V4 主动性分（新维度，仅作硬约束不计入总分）

### 4.1 为什么不进总分

主动买卖比 / 大户持仓比 这种"参与者结构"信号，**和已有的 OI/funding 高度相关但不严格独立**。如果直接加权进总分，会重复打分。  
V4 的做法：**算出来作为状态机的硬约束，避免冲突**。

### 4.2 主动性向量计算

```python
def compute_actor_vector(tc):
    """
    返回一个三元标签：(taker_dir, holder_dir, divergence)
    """
    taker_ratio = tc.get("taker_ratio", 1.0)         # 主动买卖比
    holder_ratio = tc.get("holder_ratio", 1.0)       # 大户持仓比
    holder_gap = tc.get("holder_market_gap", 0)      # 大户-市场账户gap

    # Taker 方向（散户/Taker主动方）
    if taker_ratio >= 1.5: taker_dir = "active_long"
    elif taker_ratio <= 0.67: taker_dir = "active_short"
    else: taker_dir = "neutral"

    # Holder 方向（大户多头/空头）
    if holder_ratio >= 1.3 and holder_gap > 0: holder_dir = "whale_long"
    elif holder_ratio <= 0.77 and holder_gap < 0: holder_dir = "whale_short"
    else: holder_dir = "whale_neutral"

    # 散户大户分歧（核心反指标信号）
    divergence = (
        (taker_dir == "active_long" and holder_dir == "whale_short")
        or (taker_dir == "active_short" and holder_dir == "whale_long")
    )
    return {"taker": taker_dir, "holder": holder_dir, "divergence": divergence}
```

### 4.3 用法：状态机硬约束

| 主动性向量 | 含义 | 影响 |
|-----------|------|------|
| `holder=whale_long, taker=active_short` | 大户做多 + 散户做空 = **典型诱空** | 增强"诱空"状态置信 |
| `holder=whale_long, taker=active_long` | 大户散户都做多 = **挤压追多风险** | 严禁触发"逼空主升做多"（已晚） |
| `holder=whale_short, taker=active_long` | 大户做空 + 散户追多 = **派发中** | 强制触发"高位派发"警告 |
| `divergence=True` | 大户散户对赌 | 提高"诱空"或"派发"信号置信 +20% |

---


## 五、V4 状态机（结构形态硬条件矩阵）

### 5.1 状态—结构形态映射表

把 V1 的状态机条件 **加一层 TradeCat 结构形态硬约束**：

| V1 状态 | V1 原条件 | **V4 新增 TradeCat 硬约束** |
|---------|----------|---------------------------|
| **吸筹/观察** | control < 45 | （无变化） |
| **诱空** | pos_bias=long, position≥60, structure≥55, funding<0 | **+ 结构形态 ∈ {空头共振+清仓风险, 主动买领先空, 大户领先做多} ; 排除 {结构冲突, 中性}** |
| **启动主升** | pc24h≥35%, structure≥60 | **+ 结构形态 ∈ {主动买领先多, 多头共振} ; 额连续根数≥3** |
| **逼空主升** | 三层共振 ≥ 70 + funding<0.0005 + spotLead>8% | **+ 结构形态 = 大户领先做多 ; 不能=挤压追多风险（已晚）+ taker_dir=active_long** |
| **高位派发** | pos_bias=short, near_high≥95%, funding>0.1%, pc24h>18% | **+ 结构形态 ∈ {大户领先做空, 多头共振+清仓风险} 强制触发** |
| **砸盘下杀** | pos_bias=short, position≥72, structure≥72, pc24h<-4% | **+ 风险 = 清仓风险 ; 额连续根数(空方)≥2** |
| **(新)假信号过滤** | — | **结构形态=结构冲突 ⇒ 强制回到观察** |

### 5.2 V4 状态机代码

```python
def derive_strategy_state_v4(d, tc, actor):
    """
    d  = V1/V4 三层打分结果
    tc = TradeCat 字段
    actor = compute_actor_vector(tc) 输出
    """
    control = d["controlScore"]
    position = d["positionScore"]
    structure = d["structureScore"]
    pos_bias = d["positionBias"]
    funding = tc["funding_rate"] or 0
    pc24h = d["priceChange24h"]
    near_high = d["nearHighRatio"]
    spot_lead = d.get("spotLead", 0)

    formation = tc.get("formation", "中性")            # 结构形态
    risk_label = tc.get("risk_label", "-")             # 清仓/挤压风险
    consec_amt = tc.get("consec_amt_n", 0)             # 额连续根数

    # === 0. 控盘门槛（V1 保留）===
    if control < 45:
        return {"state": "吸筹", "bias": "观察", "reason": "控盘<45"}

    # === 0.5 假信号过滤（V4 新增）===
    if formation == "结构冲突":
        return {"state": "吸筹", "bias": "观察",
                "reason": "结构冲突，等待方向明朗"}

    # === 1. 派发预警（V4 新增 michill 缺位时用）===
    if (formation == "大户领先做空" and risk_label == "清仓风险"
        and near_high >= 0.85):
        return {"state": "派发预警", "bias": "回避",
                "reason": "大户做空+高位+清仓风险"}

    # === 2. 高位派发（强化）===
    if (pos_bias == "short" and control >= 70
        and structure >= 65 and near_high >= 0.95
        and funding > 0.001 and pc24h > 0.18
        and formation in ("大户领先做空", "多头共振")
        and risk_label in ("清仓风险", "挤压/追多风险")):
        return {"state": "高位派发", "bias": "准备空",
                "riskFlags": ["过热"], "reason": "高位+大户出货+TC风险标签确认"}

    # === 3. 逼空主升（强化）===
    # 关键：必须是"大户做多 + Taker还没active_long" 的早期挤空，否则就是"已晚"
    if (pos_bias == "long" and position >= 70 and structure >= 70
        and (funding < 0.0005 or spot_lead > 0.08) and pc24h >= 0.08
        and formation == "大户领先做多"
        and risk_label != "挤压/追多风险"        # 排除已晚
        and actor["taker"] != "active_long"):    # 排除已被散户追
        return {"state": "逼空主升", "bias": "做多",
                "reason": "大户已就位，Taker未追入，挤空临界点"}

    # === 4. 启动主升（强化）===
    if (pc24h >= 0.25 and structure >= 60 and pos_bias == "long"
        and formation in ("主动买领先多", "多头共振")
        and consec_amt >= 3):
        return {"state": "启动主升", "bias": "准备多",
                "reason": "TC启动结构+连续3根+本地多头确认"}

    # === 5. 诱空（V4 强化：要求 actor 分歧）===
    if (pos_bias == "long" and position >= 60 and structure >= 55
        and funding < 0
        and formation in ("空头共振", "主动买领先空", "大户领先做多")
        and (actor["divergence"] or formation == "大户领先做多")):
        return {"state": "诱空", "bias": "准备多",
                "reason": "费率负+大户散户分歧/大户做多"}

    # === 6. 砸盘下杀 ===
    if (pos_bias == "short" and position >= 72 and structure >= 72
        and pc24h < -0.04 and risk_label == "清仓风险"):
        return {"state": "砸盘下杀", "bias": "做空",
                "reason": "结构破位+TC清仓风险确认"}

    return {"state": "吸筹", "bias": "观察", "reason": "未确认"}
```

---


## 六、CSV 实例验证

用异动面板里 5 个具有典型特征的币各跑一遍 V4 推演：

### 6.1 $TA — 多头共振 + Taker 极强主动

```yaml
TC 数据:
  综合分: 100, 强度: 极强
  方向: 买入/做多
  结构形态: 多头共振
  清仓/挤压风险: 挤压/追多风险      ← 关键
  额连续根数: 2
  主动买卖比: 21.35                ← 极强多头主动
  大户持仓比: 1.96                 ← 大户也多
  大户仓位gap: 0.0267              ← 大户略偏多
  4h额变: 1.538%, 1d额变: 3.883%
  amt_z_1d: 0.834, vol_z_1d: 0.982

V4 推演:
  actor: holder=whale_long, taker=active_long, divergence=False
  position 分（V4 z-score 化）:
    oi_pressure ≈ z(0.83)*22 + z(0.83)*8 = 17.6  
    turnover ≈ z(0.98)*20 = 9.8
    consec_bonus = 4 (n=2)
    ≈ 31 → 加 funding/basis 项后约 45
  状态机判定:
    - control 假设 60 ≥ 45 ✓
    - 结构=多头共振 ≠ 结构冲突 ✓
    - 触发"逼空主升"? 
      pos_bias=long ✓, position=45 < 70 ✗
    - 触发"启动主升"?
      pc24h=3.88% < 25% ✗ (1d涨幅不足)
    - 触发"诱空"?
      funding 不为负 ✗
    → 默认: 吸筹观察
  
关键判断: 风险=挤压/追多风险 + Taker active_long
        说明 "已经被散户追多" → 这个币其实是高位
        V4 拒绝"逼空主升"做多是对的（避免接盘）
```

### 6.2 $XLM — 空头共振 + 清仓风险

```yaml
TC 数据:
  综合分: 100, 方向: 流出/清仓
  结构形态: 空头共振
  清仓/挤压风险: 清仓风险
  4h额变: -7.36%, 1d额变: 12.76%
  主动买卖比: 0.89                ← 空方略主动
  大户持仓比: 0.82                ← 大户偏空
  
V4 推演:
  actor: holder=whale_short, taker=neutral, divergence=False
  状态机:
    - 触发"砸盘下杀"?
      pos_bias=short, pc24h<-4% (1d -7.36%) ✓
      风险=清仓风险 ✓ → 触发！
  → 砸盘下杀/做空
```

### 6.3 $HEI — 1d 持仓暴增 1501%（极端异动）

```yaml
TC 数据:
  综合分: 100, 命中 10 个榜单
  结构形态: 大户领先做多
  清仓/挤压风险: 挤压/追多风险      ← 关键
  额连续根数: 1                    ← 单根脉冲
  主动买卖比: 1.53                 ← 中等多头
  大户持仓比: 1.23, gap: 0.65
  1d额变: 1501.75%
  amt_z_1d: 5.91 (远超 2.5σ)
  
V4 推演:
  actor: holder=whale_long, taker=active_long, divergence=False
  position 分:
    oi_pressure: z(5.91)饱和→22 + 8 = 30
    turnover: 高
    consec_bonus = 0 (n=1)        ← 缺持续性
    ≈ 75
  状态机:
    - control 假设 50 ≥ 45 ✓
    - 触发"逼空主升"? 
      formation=大户领先做多 ✓
      但 risk=挤压/追多风险 ✗ (已晚)
      且 taker=active_long ✗ (散户也在追)
      → 拒绝
    - 触发"启动主升"?
      pc24h 不足 25%（绝对值小）
      consec_amt=1 < 3 ✗
      → 拒绝
  → 吸筹观察
  
关键: 异动 1501% 看似极强，但单根脉冲+挤压追多风险+散户已追入
      = 庄家已拉完，进场=接盘
      V4 正确拒绝（这是 V1/V3 都会被骗的信号）
```

### 6.4 $AR — 综合分 299（全市场快照TOP1），但主动买卖反差

```yaml
TC 数据:
  综合分(快照表): 299.57
  结构形态: 主动买领先空            ← 注意：和方向反差
  清仓/挤压风险: 挤压/追多风险
  4h额变: 1.21%, 1d额变: 0.66%, 1w额变: 18.45%
  主动买卖比: 1.22                 ← 中等多头
  大户持仓比: 1.91, gap: 0.13      ← 大户偏多
  额连续根数: 4                    ← 持续性强
  
V4 推演:
  actor: holder=whale_long (1.91), taker=neutral (1.22), divergence=False
  状态机:
    - formation=主动买领先空 (Taker层)
    - holder=whale_long (大户层)
    - 这种"主动买领先空 + 大户做多" 恰好是诱空特征
    - 触发"诱空"? 需要 funding<0
      若 funding<0 → 诱空/准备多
      若 funding≥0 → 等待
  → 诱空候选（关键看 funding）

V4 的判断比 V1 更精准: 利用了 Taker vs 大户的分歧
```

### 6.5 $TOSHI — 主动买卖比 80.24（极端值）

```yaml
TC 数据:
  综合分: 178
  结构形态: 多头共振
  主动买卖比: 80.24                ← 异常极值
  大户持仓比: 1.14
  
V4 推演:
  taker_ratio=80 是疑似数据噪音（合约小币可能薄成交导致比值爆炸）
  → V4 应在数据层做异常值过滤: ratio > 50 标 stale/不可信
  → 退化到只看 holder + funding/OI 信号
```

---


## 七、V4 关键收益（vs V1 / V3）

| 维度 | V1 | V3（仅 yss） | **V4 (融合 TC)** |
|------|----|----|----|
| 经验系数 | 全靠拍脑袋 (`*48` `*18000`) | 部分 | **z-score 全面替代，自适应不同币种** |
| 候选池 | 全市场扫描 | 全市场扫描 | **TC 异动池 ∩ 控盘≥45（缩窄到 <30/分钟）** |
| 大户/散户结构 | 缺失 | 缺失 | **Taker + Holder 双向量 + 分歧检测** |
| 状态切换约束 | 阈值 | 阈值 | **阈值 ∩ TC结构形态 ∩ 风险标签 三重约束** |
| 假信号过滤 | 无 | confirmation>500打折 | **结构冲突→强制观察 + 主动性向量校验** |
| 持续性量化 | funding 时长 | funding 时长 | **额/量连续根数（更通用）** |
| 哲学 | 阶段叙事 | 阶段叙事 + yss雷达 | **阶段叙事 + 横截面异动 双引擎** |

---

## 八、V4 输出 schema 示例

```yaml
symbol: "AR"
timestamp: "2026-05-30T14:51:00+08:00"

# 三层打分（V4 z-score 化）
scores:
  control: 60          # michill.ai 缺位时用 V1 代理
  position: 73         # 已用 z-score
  structure: 58
  total: 64.7

# V4 主动性向量
actor:
  taker: "neutral"
  holder: "whale_long"
  divergence: false

# TradeCat 透传字段
tradecat:
  formation: "主动买领先空"
  risk_label: "挤压/追多风险"
  consec_amt_n: 4
  hits: 1            # 命中榜单数
  intensity: "极强"

# 状态机输出
strategy:
  state: "诱空"
  bias: "准备多"
  reason: "费率负+主动买领先空+大户领先做多（divergence in formation）"
  confidence: 0.78    # 0-1
  
# 风控建议
risk:
  position_size_pct: 0.4     # 总仓位 5% × 0.4 = 2%
  stop_loss: -0.06
  time_stop_hours: 48
  
# 工程层
freshness: 75
warnings: []
```

---

## 九、实施清单

```
Phase 1（数据接入，3 天）
  □ TradeCat CSV → 解析为 schema_v2 dict
  □ 字段映射表实现（A 档 5 个核心字段）
  □ 异常值过滤（taker_ratio > 50 标 stale）
  □ 数据新鲜度 < 60 → 告警

Phase 2（z-score 化，2 天）
  □ 实现 z_score_to_points 工具函数
  □ 改写 compute_position_score_v4
  □ 单元测试: 用 5 个示例币验证打分变化

Phase 3（主动性向量，1 天）
  □ compute_actor_vector 实现
  □ 单元测试 4 种典型组合

Phase 4（状态机融合，3 天）
  □ derive_strategy_state_v4 实现
  □ 状态—结构形态映射表硬编码
  □ 跑 50 个历史样本对比 V1 vs V4

Phase 5（候选池筛选层，2 天）
  □ TradeCat 异动 → 控盘≥45 过滤管线
  □ 实测候选池规模（应 < 30/min）
  □ 对比扫描效率（V1 全市场 vs V4 候选池）

Phase 6（风控 + 警报，2 天）
  □ position_size 接入 confidence + flow流动性等级
  □ Telegram 警报模板
```

---

## 十、不解决的剩余缺口（待 michill.ai 接入）

V4 仍未填补的 V1 缺口：

1. **链上集群分析**（michill.ai 必备）
2. **Smart Money 钱包追踪**（michill.ai）  
3. **大户向 CEX 转账实时监控**（链上）
4. **Token 安全审计**（合约权限/Rug 概率）

> TradeCat 完全没有链上数据，V4 在控盘分这一层仍然依赖 V1 代理模型。  
> michill.ai 接入后再升级到 V5。

---

*文档版本：V4.0（深度融合）| 2026-05-30*  
*核心原则：双引擎互补 + z-score 严谨化 + 结构形态硬约束*  
*基于 TradeCat 实测 CSV 数据（异动面板 105 行 + 全市场快照 1445 行）*
